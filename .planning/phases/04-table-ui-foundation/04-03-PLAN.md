---
phase: 04-table-ui-foundation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/components/poker/molecules/PlayerSeat.tsx
  - src/components/poker/molecules/CommunityCards.tsx
  - src/components/poker/molecules/PotDisplay.tsx
autonomous: true

must_haves:
  truths:
    - "PlayerSeat displays position label, cards, stack size, and bet"
    - "CommunityCards renders flop/turn/river in horizontal row"
    - "PotDisplay shows pot amount with formatting"
  artifacts:
    - path: "src/components/poker/molecules/PlayerSeat.tsx"
      provides: "Player seat with cards, stack, bet, and state"
      exports: ["PlayerSeat"]
      min_lines: 50
    - path: "src/components/poker/molecules/CommunityCards.tsx"
      provides: "Community cards display"
      exports: ["CommunityCards"]
      min_lines: 20
    - path: "src/components/poker/molecules/PotDisplay.tsx"
      provides: "Pot amount display"
      exports: ["PotDisplay"]
      min_lines: 15
  key_links:
    - from: "src/components/poker/molecules/PlayerSeat.tsx"
      to: "src/components/poker/atoms/Card.tsx"
      via: "import and rendering"
      pattern: "import.*Card.*from.*atoms"
    - from: "src/components/poker/molecules/CommunityCards.tsx"
      to: "src/components/poker/atoms/Card.tsx"
      via: "map over cards and render"
      pattern: "map.*Card"
---

<objective>
Create molecular poker UI components (PlayerSeat, CommunityCards, PotDisplay) that compose atomic components into functional units.

Purpose: These molecules are the building blocks for the PokerTable organism. They encapsulate player state, community card display, and pot visualization.
Output: Three molecular components ready for integration into the table layout.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-table-ui-foundation/04-CONTEXT.md
@.planning/phases/04-table-ui-foundation/04-RESEARCH.md
@.planning/phases/04-table-ui-foundation/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create PlayerSeat component</name>
  <files>src/components/poker/molecules/PlayerSeat.tsx</files>
  <action>
Create PlayerSeat component that displays position, cards, stack, bet, and player state:

**src/components/poker/molecules/PlayerSeat.tsx:**
```typescript
import { cn } from '@/lib/utils';
import { Card } from '../atoms/Card';
import { CardBack } from '../atoms/CardBack';
import { Chip } from '../atoms/Chip';

interface PlayerSeatProps {
  position: 'BTN' | 'SB' | 'BB' | 'UTG' | 'HJ' | 'CO';
  stackBB: number;
  cards?: Array<{ rank: string; suit: 'h' | 'd' | 'c' | 's' }>;
  bet?: number; // Current bet amount in BB
  isActive: boolean; // Player is in hand
  isFolded: boolean; // Player has folded
  isHero?: boolean; // Highlight hero seat
  showCards?: boolean; // Show face-up cards (hero) or face-down (villain)
  className?: string;
}

export function PlayerSeat({
  position,
  stackBB,
  cards,
  bet,
  isActive,
  isFolded,
  isHero = false,
  showCards = false,
  className,
}: PlayerSeatProps) {
  return (
    <div
      className={cn(
        'flex flex-col items-center gap-2 transition-opacity',
        isFolded && 'opacity-40',
        isHero && 'relative',
        className
      )}
    >
      {/* Hero highlight ring */}
      {isHero && (
        <div className="absolute inset-0 -m-2 rounded-lg border-2 border-blue-500 animate-pulse" />
      )}

      {/* Position label */}
      <span
        className={cn(
          'text-xs font-bold px-2 py-1 rounded-md',
          isActive ? 'bg-green-600 text-white' : 'bg-gray-600 text-gray-300'
        )}
      >
        {position}
      </span>

      {/* Cards */}
      {cards && cards.length > 0 && !isFolded && (
        <div className="flex gap-1">
          {showCards ? (
            cards.map((card, i) => (
              <Card key={i} rank={card.rank} suit={card.suit} size="sm" />
            ))
          ) : (
            <>
              <CardBack size="sm" />
              <CardBack size="sm" />
            </>
          )}
        </div>
      )}

      {/* Bet chips */}
      {bet !== undefined && bet > 0 && (
        <Chip amount={bet} size="sm" />
      )}

      {/* Stack size */}
      <span
        className={cn(
          'text-xs font-semibold px-2 py-0.5 rounded-md',
          isActive ? 'bg-gray-800 text-white' : 'bg-gray-600 text-gray-400'
        )}
      >
        {stackBB.toFixed(1)} BB
      </span>
    </div>
  );
}
```

Note: PlayerSeat handles both hero (face-up cards) and villain (face-down) seats. isHero prop adds pulsing border highlight. isFolded dims the entire seat. Stack size shows one decimal place for precision.
  </action>
  <verify>
Check file exists and imports atoms:
```bash
cat src/components/poker/molecules/PlayerSeat.tsx | grep -E "export function PlayerSeat"
cat src/components/poker/molecules/PlayerSeat.tsx | grep -E "import.*Card.*from.*atoms"
```
Should return matching lines.
  </verify>
  <done>PlayerSeat.tsx exists, imports Card/CardBack/Chip atoms, and handles hero/villain/folded states</done>
</task>

<task type="auto">
  <name>Create CommunityCards and PotDisplay components</name>
  <files>
    src/components/poker/molecules/CommunityCards.tsx
    src/components/poker/molecules/PotDisplay.tsx
  </files>
  <action>
Create CommunityCards component for board card display:

**src/components/poker/molecules/CommunityCards.tsx:**
```typescript
import { cn } from '@/lib/utils';
import { Card } from '../atoms/Card';

interface CommunityCardsProps {
  cards: Array<{ rank: string; suit: 'h' | 'd' | 'c' | 's' }>;
  className?: string;
}

export function CommunityCards({ cards, className }: CommunityCardsProps) {
  if (cards.length === 0) return null;

  return (
    <div className={cn('flex gap-2', className)}>
      {cards.map((card, i) => (
        <Card key={i} rank={card.rank} suit={card.suit} size="md" />
      ))}
    </div>
  );
}
```

Create PotDisplay component:

**src/components/poker/molecules/PotDisplay.tsx:**
```typescript
import { cn } from '@/lib/utils';

interface PotDisplayProps {
  amount: number; // Pot amount in BB
  className?: string;
}

export function PotDisplay({ amount, className }: PotDisplayProps) {
  return (
    <div className={cn('flex flex-col items-center gap-1', className)}>
      <span className="text-xs font-semibold text-gray-400 uppercase">Pot</span>
      <span className="text-2xl font-bold text-white bg-gray-800 px-4 py-2 rounded-lg shadow-md">
        {amount.toFixed(1)} BB
      </span>
    </div>
  );
}
```

Note: CommunityCards maps over cards array and renders Card atoms. Returns null if empty (preflop). PotDisplay shows formatted amount with one decimal place and "Pot" label.
  </action>
  <verify>
Check files exist and exports are correct:
```bash
cat src/components/poker/molecules/CommunityCards.tsx | grep -E "export function CommunityCards"
cat src/components/poker/molecules/PotDisplay.tsx | grep -E "export function PotDisplay"
```
Check Card import in CommunityCards:
```bash
cat src/components/poker/molecules/CommunityCards.tsx | grep "import.*Card.*from"
```
All should return matching lines.
  </verify>
  <done>CommunityCards.tsx and PotDisplay.tsx exist with proper atom composition</done>
</task>

</tasks>

<verification>
Visual check - create test page to render molecules:

1. Create src/app/test-molecules/page.tsx with all three components
2. Use sample data:
   - PlayerSeat with hero cards (As, Kh), stack 100 BB, bet 5 BB
   - PlayerSeat with villain (face-down), folded state
   - CommunityCards with flop (Qs, Jh, Td)
   - PotDisplay with 15.5 BB
3. Run `npm run dev`
4. Visit http://localhost:3000/test-molecules
5. Verify:
   - Hero seat shows cards face-up with pulsing border
   - Villain seat shows card backs, dimmed if folded
   - Community cards display in row
   - Pot shows amount with formatting
   - Toggle theme - all components adapt colors

Delete test page after verification.
</verification>

<success_criteria>
- Three molecular components exist in src/components/poker/molecules/
- PlayerSeat renders position, cards, stack, bet, and state
- PlayerSeat handles hero (showCards=true) vs villain (showCards=false)
- PlayerSeat dims when isFolded=true
- PlayerSeat highlights when isHero=true
- CommunityCards renders horizontal row of cards
- CommunityCards returns null when cards array is empty
- PotDisplay shows formatted pot amount with label
- All components import and use atomic components
- All components are theme-aware
</success_criteria>

<output>
After completion, create `.planning/phases/04-table-ui-foundation/04-03-SUMMARY.md`
</output>
