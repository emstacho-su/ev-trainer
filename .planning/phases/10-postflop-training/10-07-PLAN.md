---
phase: 10-postflop-training
plan: "07"
type: execute
wave: 5
depends_on: ["10-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can train flop c-bet scenarios as IP aggressor"
    - "User can train turn barrel / check-back scenarios"
    - "User can train river value-bet / bluff / check decisions"
    - "Multi-street hand progression flows from flop to turn to river"
    - "Postflop action history shows bet sizes and context labels"
  artifacts:
    - path: "src/app/postflop-training/page.tsx"
      provides: "Accessible, functional postflop training page"
    - path: "src/components/training/PostflopTrainingSession.tsx"
      provides: "Working multi-street training session"
    - path: "src/components/training/PostflopHandSummary.tsx"
      provides: "Hand summary with per-street decisions"
  key_links:
    - from: "src/app/postflop-training/page.tsx"
      to: "src/components/training/PostflopTrainingSession.tsx"
      via: "rendered in training phase"
      pattern: "PostflopTrainingSession"
---

<objective>
User acceptance verification for Phase 10 postflop training. Test the complete multi-street training flow from flop to river, verify EV grading works correctly, check off-solver-line handling, and confirm hand summary + replay features work.

Purpose: Phase 10 is the final phase — verifying it end-to-end confirms the entire v3 platform is complete.
Output: Verified working postflop training experience ready for production.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-postflop-training/10-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 10 postflop training implementation:
    - Multi-street training (flop, turn, river) with solver-graded decisions
    - Bet sizing buttons showing % and BB amount
    - Off-solver-line indicator when hero deviates
    - Villain actions with animation delay
    - Community card sequential reveal
    - Hand summary with per-street EV review
    - Replay option
    - POST-05 spot context labels ("SRP IP Aggressor")
    - POST-06 last raiser indicator (LRI token)
  </what-built>
  <how-to-verify>
    Ensure both servers are running:
    - Terminal 1: npm run dev (Next.js on port 3000)
    - Terminal 2: npm run server:dev (Express on port 4000)

    Test 1: Basic flop training
    1. Navigate to http://localhost:3000/postflop-training
    2. Verify: board cards deal sequentially with stagger (3 cards appear one at a time)
    3. Verify: "SRP IP Aggressor" or similar context label appears below table
    4. Verify: Action buttons show "Bet 75% (9.0 BB)" format with BB amounts
    5. Verify: Check button appears in same row as bet buttons
    6. Select any action — verify EV feedback expands inline showing frequency and EV value

    Test 2: Multi-street progression
    1. Select "Check" on the flop
    2. Verify: villain acts after ~0.7s with a labeled action
    3. Verify: "Turn" dealing animation begins — 4th card appears on board
    4. Verify: New action buttons appear for turn decision
    5. Select any action on turn — verify EV feedback
    6. Verify river deals correctly (5th card appears)
    7. Select action on river — verify hand complete triggers

    Test 3: Off-solver-line indicator
    1. Start a new hand
    2. Select the lowest-frequency action (the action with lowest % shown post-reveal)
    3. Verify: yellow "Off Solver Line" badge appears on subsequent streets
    4. Verify: EV shows as "N/A" in yellow text for the off-line street

    Test 4: Hand summary
    1. Complete a full hand (flop + turn + river)
    2. Verify: Hand summary screen appears showing 3 rows (Flop, Turn, River)
    3. Verify: Each row shows the action taken and EV value
    4. Verify: "Total EV Impact" stat shows at bottom
    5. Click "Replay Hand" — verify replay mode steps through each street
    6. Click "Next Hand" — verify new hand starts with new board cards

    Test 5: LRI token
    1. Start a hand where heroRole is "aggressor"
    2. Verify: small "LRI" orange token appears near hero's seat position
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed with details</resume-signal>
</task>

</tasks>

<verification>
All 5 manual test flows pass without errors.
No console errors during normal training flow.
</verification>

<success_criteria>
- POST-01: Flop decisions work with bet sizing options (Bet 33%, 50%, 75%, 100%, All-in)
- POST-02: Turn decisions continue from flop with updated board
- POST-03: River decisions work with showdown logic leading to hand complete
- POST-04: Full flop -> turn -> river progression works in single session
- POST-05: "SRP IP Aggressor", "3b OOP Caller" etc. labels visible above action buttons
- POST-06: LRI "Last Raiser" token visible on aggressor seat
- Off-solver-line: yellow badge appears, N/A shown for EV on deviated streets
- Hand summary: per-street table with actions, EVs, and total EV impact
- Replay: steps through each street's board and decision
</success_criteria>

<output>
After completion, create `.planning/phases/10-postflop-training/10-07-SUMMARY.md`
</output>
