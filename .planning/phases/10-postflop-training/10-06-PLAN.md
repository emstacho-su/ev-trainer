---
phase: 10-postflop-training
plan: "06"
type: execute
wave: 4
depends_on: ["10-05"]
files_modified:
  - src/components/training/PostflopHandSummary.tsx
  - src/app/postflop-training/page.tsx
autonomous: true

must_haves:
  truths:
    - "Hand summary screen shows per-street decisions with EV loss for each street"
    - "Replay option in hand summary allows stepping through each street's board and decision"
    - "Postflop training page is accessible at /postflop-training and renders PostflopTrainingSession"
    - "Hand summary shows 'Off Solver Line' indicator for streets where hero deviated"
    - "Auto-advance starts new hand after summary dismissal (with replay or skip)"
  artifacts:
    - path: "src/components/training/PostflopHandSummary.tsx"
      provides: "Hand summary screen with per-street review and replay option"
      exports: ["PostflopHandSummary"]
    - path: "src/app/postflop-training/page.tsx"
      provides: "Next.js page wiring PostflopTrainingSession + HandSummary into training loop"
  key_links:
    - from: "src/app/postflop-training/page.tsx"
      to: "src/components/training/PostflopTrainingSession.tsx"
      via: "import PostflopTrainingSession"
      pattern: "PostflopTrainingSession"
    - from: "src/app/postflop-training/page.tsx"
      to: "src/components/training/PostflopHandSummary.tsx"
      via: "import PostflopHandSummary"
      pattern: "PostflopHandSummary"
    - from: "src/components/training/PostflopHandSummary.tsx"
      to: "src/lib/engine/postflopStateMachine.ts"
      via: "import StreetDecision"
      pattern: "StreetDecision"
---

<objective>
Build the hand summary screen (shown after each complete multi-street hand) and the postflop training page that ties the full flow together. The summary shows per-street decisions, EV outcomes, and offers a replay option.

Purpose: CONTEXT.md specifies "Hand summary screen shown after each hand before advancing" and "replay option to step through each street." This plan closes that loop and creates the training page entry point.
Output: PostflopHandSummary component + /postflop-training page with full training loop.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-postflop-training/10-CONTEXT.md
@src/components/training/PostflopTrainingSession.tsx
@src/lib/engine/postflopStateMachine.ts
@src/lib/engine/postflopSpot.ts
@src/app/preflop-training/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostflopHandSummary component</name>
  <files>src/components/training/PostflopHandSummary.tsx</files>
  <action>
    Create PostflopHandSummary.tsx as "use client" component.

    Props:
    ```typescript
    interface PostflopHandSummaryProps {
      decisions: StreetDecision[];  // from postflopStateMachine.ts
      spot: PostflopSpot;           // original spot
      onNextHand: () => void;       // start new hand
      onReplay: () => void;         // step through replay
    }
    ```

    Layout (dark theme, card-style container matching existing summary patterns):

    1. Header: "Hand Complete" title + spot context label (call deriveSpotContextLabel)

    2. Street rows table: for each StreetDecision in decisions:
       - Street name (FLOP / TURN / RIVER)
       - Hero action taken (formatted: "Check", "Bet 75%", "Fold")
       - EV column: if isOffLine show "N/A" in yellow text, else show grade.evUser formatted "+X.XX BB" (green) or "-X.XX BB" (red)
       - Match column: if grade.isBestAction show "Best" badge in green, else show "Suboptimal" in yellow, else "Off Line" in red

    3. Off-solver-line section: if any decision has isOffLine, show an explanation box:
       "You deviated from the solver's optimal line. Streets after your deviation show N/A for EV — the solver can't grade decisions on non-optimal lines."

    4. Summary stat: "Total EV Impact" — sum of all non-N/A evLossVsBest values formatted as "0.00 BB" or "-X.XX BB"

    5. Two buttons in bottom row:
       - "Replay Hand" -> onReplay() (outlined style)
       - "Next Hand" -> onNextHand() (primary blue button)

    Tailwind styling: match existing summary/stats cards (bg-gray-800, border-gray-700, rounded-lg). Street rows use divide-y divide-gray-700.
    Reference src/components/SummaryStatsCards.tsx and src/app/summary/ for visual patterns.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "PostflopHandSummary" || echo "No errors"</verify>
  <done>Component compiles. Shows per-street table with EV values. N/A shown for off-line streets. Next Hand and Replay buttons present.</done>
</task>

<task type="auto">
  <name>Task 2: Postflop training page + training loop</name>
  <files>src/app/postflop-training/page.tsx</files>
  <action>
    Create src/app/postflop-training/page.tsx.

    Study src/app/preflop-training/page.tsx for the page pattern and follow it closely.

    Page state:
    ```typescript
    type PagePhase = "training" | "summary" | "replay";
    ```

    State:
    - pagePhase: PagePhase (starts "training")
    - currentSpot: PostflopSpot | null (generated on mount and after each hand)
    - completedDecisions: StreetDecision[] (set when hand completes)
    - replayIndex: number (for replay stepping, 0-based street index)

    On mount: generate a PostflopSpot using generatePostflopBoard() from postflopSpot.ts.
    Create a sample spot hardcoded for initial development:
    ```typescript
    function createSampleSpot(): PostflopSpot {
      const board = generatePostflopBoard();
      const texture = classifyBoardTexture(board);
      return {
        spotId: crypto.randomUUID(),
        street: "FLOP",
        board,
        potBb: 12,
        effectiveStackBb: 88,
        heroPosition: "IP",
        heroRole: "aggressor",
        heroHand: ["Ah", "Kd"],   // TODO: will come from preflop range in future
        preflopActionType: "srp",
        preflopHistory: ["UTG", "BTN_CALL"],
        texture,
      };
    }
    ```

    Flow:
    - pagePhase "training": render PostflopTrainingSession with currentSpot, onHandComplete sets completedDecisions and pagePhase to "summary"
    - pagePhase "summary": render PostflopHandSummary with completedDecisions, onNextHand calls generateNewSpot() and sets pagePhase back to "training", onReplay sets pagePhase to "replay" and replayIndex to 0
    - pagePhase "replay": render a simple replay view showing the board state and decision at replayIndex. "Previous" and "Next" buttons advance replayIndex. "Back to Summary" button returns to summary.

    Replay view (simple implementation): show board cards up to the street's length, show the action taken, show the EV value. Just a read-only display of each StreetDecision.

    Page metadata: export const metadata = { title: "Postflop Training | EV Trainer" }
    Add "use client" (needed for useState hooks on the page).

    Also create the directory: src/app/postflop-training/ (just the page.tsx file needed).
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "postflop-training" || echo "No errors"; ls /c/Users/estac/projects/ev-trainer/src/app/postflop-training/ 2>/dev/null && echo "Dir exists"</verify>
  <done>Page exists at src/app/postflop-training/page.tsx. Compiles cleanly. Renders PostflopTrainingSession in training phase and PostflopHandSummary in summary phase.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — no TypeScript errors.
Navigate to http://localhost:3000/postflop-training — page loads, PostflopTrainingSession renders.
</verification>

<success_criteria>
- /postflop-training page renders PostflopTrainingSession
- After hand completes, PostflopHandSummary shows with per-street table
- Per-street rows show action taken, EV value or N/A for off-line streets
- "Next Hand" button starts new hand with new board
- "Replay Hand" button enters replay mode showing each street
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-postflop-training/10-06-SUMMARY.md`
</output>
