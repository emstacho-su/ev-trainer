---
phase: 10-postflop-training
plan: "06"
type: execute
wave: 5
depends_on: ["10-05"]
files_modified:
  - src/components/poker/molecules/HandSummaryModal.tsx
  - src/app/postflop-training/page.tsx
autonomous: true

must_haves:
  truths:
    - "Hand summary modal shows per-street decisions with EV and solver match status"
    - "Deviation streets clearly marked in summary (off-solver-line badge)"
    - "User can replay the hand from summary (steps through each street)"
    - "/postflop-training page renders PostflopTrainingSession and loops to next hand"
  artifacts:
    - path: "src/components/poker/molecules/HandSummaryModal.tsx"
      provides: "Post-hand summary showing all three streets' decisions"
      exports: ["HandSummaryModal"]
    - path: "src/app/postflop-training/page.tsx"
      provides: "Next.js page with PostflopTrainingSession + training loop"
      exports: ["default (page component)"]
  key_links:
    - from: "src/app/postflop-training/page.tsx"
      to: "src/components/poker/organisms/PostflopTrainingSession.tsx"
      via: "renders PostflopTrainingSession"
      pattern: "PostflopTrainingSession"
    - from: "src/components/poker/molecules/HandSummaryModal.tsx"
      to: "src/lib/postflop/types.ts"
      via: "HandSummaryData prop"
      pattern: "HandSummaryData"
---

<objective>
Create the hand summary modal and the /postflop-training page that completes the training loop.

Purpose: After playing flop+turn+river, users need to review their decisions before moving to the next hand. The summary shows all three streets, marks deviations, and offers a replay option. The /postflop-training page wires everything into a complete training session with auto-advance to next hand.

Output: HandSummaryModal shows per-street review; /postflop-training page provides the complete training loop.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-postflop-training/10-CONTEXT.md
@.planning/phases/10-postflop-training/10-05-SUMMARY.md
@src/lib/postflop/types.ts
@src/lib/postflop/session/postflopSession.ts
@src/app/page.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: HandSummaryModal component</name>
  <files>src/components/poker/molecules/HandSummaryModal.tsx</files>
  <action>
    Create src/components/poker/molecules/HandSummaryModal.tsx:
    - 'use client'
    - Props: { summary: HandSummaryData; onNextHand: () => void; onReplay: () => void }
    - Renders as a native HTML &lt;dialog&gt; (following Phase 7 decision: 07-04 used native dialog)
    - Open when summary is not null — use useEffect to call showModal() imperatively
    - Layout:
      - Header: "Hand Complete" title + close/next-hand button (X)
      - Board cards: display all 5 community cards (small Card atoms)
      - Per-street sections (3 sections: Flop, Turn, River):
        - Street label
        - Hero's action: "You: Check" or "You: Bet 75%"
        - Solver match: green checkmark if isOnSolverLine, orange warning if not
        - If not on solver line: show all available actions with their EV (compact version)
        - If deviatedStreet is this street: show "Off solver line" badge
        - If street comes after deviatedStreet: show "N/A (deviation carried from {deviatedStreet})"
      - Footer: "Next Hand" button (calls onNextHand) + "Replay" button (calls onReplay)
    - Style: dark modal, bg-gray-900, border border-gray-700, rounded-xl
    - Modal backdrop: semi-transparent overlay (dialog::backdrop or CSS)
    - Use existing Card atom for board card display

    Replay behavior: onReplay callback dispatches RESET to state machine (handled in parent).
    onNextHand: dispatches RESET then startNewHand (handled in parent).
  </action>
  <verify>npx tsc --noEmit exits 0</verify>
  <done>HandSummaryModal renders with 3-street review, correct deviation markers, Next Hand and Replay buttons</done>
</task>

<task type="auto">
  <name>Task 2: /postflop-training page with training loop</name>
  <files>src/app/postflop-training/page.tsx</files>
  <action>
    Create src/app/postflop-training/page.tsx:
    - export default function PostflopTrainingPage()
    - Metadata: title "Postflop Training | EV Trainer"
    - Full-page dark layout matching src/app/page.tsx pattern
    - Renders PostflopTrainingSession component
    - When machineState === 'summary' (from session state), show HandSummaryModal:
      - onNextHand: dispatch RESET + call startNewHand()
      - onReplay: dispatch RESET (state machine goes back to deciding at flop for same hand data)
    - Page-level navigation: back arrow link to "/" (home page)
    - Auto-advance: after summary modal closes via "Next Hand", auto-start new hand
    - Note: Use PostflopSessionProvider around the whole page content OR verify it's already inside PostflopTrainingSession (plan 05 wraps it) — don't double-wrap

    The page structure:
    - 'use client' (needs access to session state for summary modal)
    - Import PostflopTrainingSession and PostflopSessionProvider
    - Import usePostflopSession to read machineState and summary
    - Render PostflopSessionProvider wrapping the page content
    - Inner component reads state and shows HandSummaryModal conditionally

    Check: If PostflopTrainingSession already wraps its own PostflopSessionProvider, the page should NOT add another wrapper. Read PostflopTrainingSession.tsx first and adjust accordingly.
  </action>
  <verify>
    npm run build (Next.js build succeeds)
    npm run dev — visit http://localhost:3000/postflop-training — page renders without error
  </verify>
  <done>/postflop-training page renders PostflopTrainingSession; summary modal appears after river; Next Hand starts new hand; Replay resets to flop</done>
</task>

</tasks>

<verification>
1. npm run build exits 0 (no build errors)
2. /postflop-training page accessible
3. HandSummaryModal renders with correct deviation marking
4. Next Hand button triggers new hand generation
</verification>

<success_criteria>
- HandSummaryModal: shows flop/turn/river decision review, deviation badge on deviated street, N/A with reason for post-deviation streets
- /postflop-training page: renders, training loop works (hand -> summary -> next hand)
- Replay button: resets to flop view for same board
- No TypeScript errors, no build errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-postflop-training/10-06-SUMMARY.md`
</output>
