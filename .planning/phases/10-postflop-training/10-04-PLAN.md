---
phase: 10-postflop-training
plan: "04"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/poker/molecules/PostflopActionButtons.tsx
  - src/components/poker/molecules/OffSolverLineBadge.tsx
  - src/components/poker/molecules/SpotContextLabel.tsx
  - src/components/poker/molecules/LastRaiserIndicator.tsx
autonomous: true

must_haves:
  truths:
    - "Bet sizing buttons show both % of pot and BB amount (e.g., 'Bet 75% (6.2 BB)')"
    - "Check option appears in same row as bet options"
    - "Off-solver-line indicator is visible and has tooltip explaining why N/A appears"
    - "Spot context label displays correctly (e.g., '3b IP Aggressor')"
    - "Last Raiser Indicator (LRI) token appears next to aggressor seat"
  artifacts:
    - path: "src/components/poker/molecules/PostflopActionButtons.tsx"
      provides: "Bet/check/fold/call/raise action buttons with BB + % labels and post-action EV expand"
      exports: ["PostflopActionButtons"]
    - path: "src/components/poker/molecules/OffSolverLineBadge.tsx"
      provides: "Off-solver-line indicator with tooltip"
      exports: ["OffSolverLineBadge"]
    - path: "src/components/poker/molecules/SpotContextLabel.tsx"
      provides: "POST-05 context label display"
      exports: ["SpotContextLabel"]
    - path: "src/components/poker/molecules/LastRaiserIndicator.tsx"
      provides: "POST-06 LRI token"
      exports: ["LastRaiserIndicator"]
  key_links:
    - from: "src/components/poker/molecules/PostflopActionButtons.tsx"
      to: "src/lib/engine/postflopSpot.ts"
      via: "import HeroPosition"
      pattern: "PostflopSpot|HeroPosition"
---

<objective>
Build the postflop-specific UI components: action buttons with bet sizing labels, off-solver-line indicator, spot context label (POST-05), and last raiser indicator token (POST-06). These are standalone molecules that the PostflopTrainingSession organism will compose.

Purpose: Postflop bet sizing requires richer button labels than preflop (% and BB amount both visible). POST-05 context labels and POST-06 LRI token are postflop-only UI requirements.
Output: Four React component files ready for use in PostflopTrainingSession.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-postflop-training/10-CONTEXT.md
@src/lib/engine/postflopSpot.ts
@src/components/poker/molecules
@src/components/poker/organisms/ActionPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostflopActionButtons component</name>
  <files>src/components/poker/molecules/PostflopActionButtons.tsx</files>
  <action>
    Create PostflopActionButtons.tsx as a "use client" component.

    Props interface:
    ```typescript
    interface PostflopAction {
      actionId: string;        // e.g. "CHECK", "BET_75", "FOLD", "CALL", "RAISE_2.2", "ALL_IN"
      label: string;           // e.g. "Check", "Bet 75%", "Fold", "Call", "Raise 2.2x", "All-in"
      bbAmount?: number;       // e.g. 7.5 (computed from pot)
      frequency?: number;      // shown post-reveal (0-1)
      ev?: number;             // shown post-reveal in BB
    }

    interface PostflopActionButtonsProps {
      actions: PostflopAction[];
      potBb: number;
      onAction: (actionId: string) => void;
      revealed: boolean;       // true after hero submits
      selectedActionId?: string;
    }
    ```

    Layout: single row flex-wrap with gap-2. Buttons use same 5-state machine as ActionButton (idle, selected, revealed-correct, revealed-incorrect style from 04-04).
    - Button text: "{label}" on first line, if bbAmount > 0 show "({bbAmount.toFixed(1)} BB)" as secondary smaller text below
    - When revealed: button expands to show frequency bar + EV below (same pattern as preflop ActionButton revealed state)
    - Use Tailwind: bg-gray-700 idle, bg-blue-600 selected, revealed adds frequency bar in green
    - fold button: bg-red-900/40 idle, bg-red-600 selected
    - check button: bg-green-900/40 idle, bg-green-600 selected
    - Disable all buttons when revealed=true

    Follow existing ActionButton styling from src/components/poker/molecules/ActionButton.tsx for consistency.
    Look at the existing component for exact Tailwind classes to reuse.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "PostflopActionButtons" || echo "No errors"</verify>
  <done>Component compiles. Shows bet sizes with BB amounts. Expands on reveal to show frequency + EV.</done>
</task>

<task type="auto">
  <name>Task 2: OffSolverLineBadge, SpotContextLabel, LastRaiserIndicator</name>
  <files>src/components/poker/molecules/OffSolverLineBadge.tsx, src/components/poker/molecules/SpotContextLabel.tsx, src/components/poker/molecules/LastRaiserIndicator.tsx</files>
  <action>
    Create OffSolverLineBadge.tsx:
    - Props: { isOffLine: boolean }
    - When isOffLine: render a yellow badge "Off Solver Line" with a tooltip (title attribute) "You deviated from the solver's optimal line. EV values are shown as N/A for remaining streets."
    - When not offLine: render nothing (return null)
    - Tailwind: inline-flex items-center gap-1 px-2 py-0.5 rounded text-xs font-medium bg-yellow-900/50 text-yellow-300 border border-yellow-700/50
    - Include a warning icon: text character "⚠" before the text
    - Wrap in a div with relative group for tooltip: hidden absolute below with same warning message (use CSS tooltip pattern matching existing codebase)

    Create SpotContextLabel.tsx:
    - Props: { label: string; street: string }
    - Render the label string (e.g. "3b IP Aggressor") in a small pill
    - Tailwind: text-xs text-gray-400 bg-gray-800 px-2 py-0.5 rounded border border-gray-700
    - Shows above the action buttons area

    Create LastRaiserIndicator.tsx:
    - Props: { isLastRaiser: boolean; position: string }
    - When isLastRaiser: render a small "LRI" token badge next to position
    - Token: text-xs font-bold text-orange-400 bg-orange-900/30 px-1 py-0.5 rounded border border-orange-700/40 ml-1
    - Title attribute: "Last Raiser - this player was the preflop aggressor"
    - When not isLastRaiser: return null

    All three are purely presentational, no state needed.
    Check existing molecule components for naming/export conventions.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "OffSolverLine|SpotContext|LastRaiser" || echo "No errors in badge components"</verify>
  <done>All three components compile. OffSolverLineBadge shows only when isOffLine=true. SpotContextLabel displays label string in pill. LastRaiserIndicator shows LRI badge when isLastRaiser=true.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — no TypeScript errors in all four new component files.
Inspect each component manually: correct props interface, correct Tailwind classes, correct conditional rendering.
</verification>

<success_criteria>
- PostflopActionButtons: shows label + BB amount, reveals frequency + EV after selection
- OffSolverLineBadge: yellow badge with tooltip, returns null when not off-line
- SpotContextLabel: pill with context string, text-xs gray styling
- LastRaiserIndicator: orange "LRI" badge, returns null when not last raiser
- All components follow existing Tailwind/dark theme conventions
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-postflop-training/10-04-SUMMARY.md`
</output>
