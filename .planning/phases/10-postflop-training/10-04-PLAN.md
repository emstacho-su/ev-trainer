---
phase: 10-postflop-training
plan: "04"
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - src/components/poker/molecules/StreetActionPanel.tsx
  - src/components/poker/molecules/PostflopActionButton.tsx
  - src/components/poker/molecules/SpotContextLabel.tsx
  - src/components/poker/molecules/LastRaiserIndicator.tsx
autonomous: true

must_haves:
  truths:
    - "StreetActionPanel shows Check/Bet/Fold/Call/Raise buttons based on solver actions"
    - "After user selects, all actions expand to show EV and frequency inline"
    - "Off-solver-line badge appears when isOnSolverLine is false"
    - "SpotContextLabel shows '3b IP Aggressor', 'SRP OOP Caller' etc based on spot"
    - "LastRaiserIndicator token renders next to aggressor seat position"
  artifacts:
    - path: "src/components/poker/molecules/StreetActionPanel.tsx"
      provides: "Bet size preset buttons + Check inline + EV reveal"
      exports: ["StreetActionPanel"]
    - path: "src/components/poker/molecules/PostflopActionButton.tsx"
      provides: "Single action button with expanded EV/frequency display after selection"
      exports: ["PostflopActionButton"]
    - path: "src/components/poker/molecules/SpotContextLabel.tsx"
      provides: "Text label describing spot type"
      exports: ["SpotContextLabel"]
    - path: "src/components/poker/molecules/LastRaiserIndicator.tsx"
      provides: "LRI chip/token component"
      exports: ["LastRaiserIndicator"]
  key_links:
    - from: "src/components/poker/molecules/StreetActionPanel.tsx"
      to: "src/lib/postflop/session/postflopSession.ts"
      via: "usePostflopSession() to read decisions and dispatch USER_DECIDED"
      pattern: "usePostflopSession"
    - from: "src/components/poker/molecules/PostflopActionButton.tsx"
      to: "src/lib/engine/solverAdapter.ts"
      via: "SolverActionOutput prop type"
      pattern: "SolverActionOutput"
---

<objective>
Build the postflop decision UI components: action buttons with EV reveal, off-solver-line badge, spot context label, and LRI token.

Purpose: These are the core interactive elements the user sees during postflop training. They must handle the pre-decision state (showing available actions) and post-decision state (showing EV for all actions inline). The off-solver-line badge is critical UX — users need to understand why subsequent streets show N/A.

Output: 4 molecule components ready for composition into PostflopTrainingSession.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-postflop-training/10-CONTEXT.md
@.planning/phases/10-postflop-training/10-03-SUMMARY.md
@src/components/poker/molecules/ActionButton.tsx
@src/lib/postflop/session/postflopSession.ts
@src/lib/engine/solverAdapter.ts
@src/lib/postflop/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostflopActionButton and StreetActionPanel</name>
  <files>
    src/components/poker/molecules/PostflopActionButton.tsx
    src/components/poker/molecules/StreetActionPanel.tsx
  </files>
  <action>
    Create src/components/poker/molecules/PostflopActionButton.tsx:
    - 'use client'
    - Props: { actionId: string; label: string; frequency?: number; ev?: number; potBb: number; isSelected: boolean; isRevealed: boolean; isDeviated: boolean; onClick: () => void; disabled?: boolean }
    - label is human-readable: "Check", "Bet 33% (Xbb)", "Bet 75% (Xbb)", "Call", "Fold", "All-in"
    - Pre-decision state: shows label, full width, clickable. Style follows existing ActionButton: bg-gray-700, hover:bg-gray-600, rounded
    - Post-decision (isRevealed=true): expands to show frequency bar + EV. isSelected uses green/red border. Non-selected show gray frequency bar.
    - Frequency bar: thin horizontal bar at bottom of button (like existing ActionButton pattern from Phase 4)
    - EV display: "+X.XX BB" format (toFixed(2)), green if positive, red if negative, gray if N/A (isDeviated=true)
    - If isDeviated=true (deviatedStreet set): show "N/A" with a small orange warning icon instead of EV
    - Disabled when isRevealed=true (can't re-click after feedback) following 04-04 decision

    Create src/components/poker/molecules/StreetActionPanel.tsx:
    - 'use client'
    - Props: { actions: SolverActionOutput[]; selectedActionId: string | null; isRevealed: boolean; isDeviated: boolean; potBb: number; onAction: (actionId: string) => void; disabled?: boolean }
    - Renders a row of PostflopActionButton components, one per action in actions[]
    - Compute label from actionId: "CHECK" -> "Check", "FOLD" -> "Fold", "CALL" -> "Call", "BET_33" -> "Bet 33%", "BET_50" -> "Bet 50%", "BET_75" -> "Bet 75%", "BET_100" -> "Bet 100%", "RAISE_2.2" -> "Raise 2.2x", "RAISE_2.5" -> "Raise 2.5x", "RAISE_3" -> "Raise 3x", "ALL_IN" -> "All-in"
    - For bet actions, append " (Xbb)" where X = (betPct * potBb).toFixed(1) — derive betPct from actionId: BET_33=0.33, BET_50=0.50, BET_75=0.75, BET_100=1.0
    - Wrap in a div with flex gap-2 layout (consistent with ActionPanel)
    - If actions is empty, show "Computing GTO strategy..." spinner (loading state)
    - If disabled=true, all buttons disabled
  </action>
  <verify>npx tsc --noEmit (no errors in these files)</verify>
  <done>StreetActionPanel renders buttons for each action; post-reveal shows frequency bars and EV; isDeviated shows "N/A" warning</done>
</task>

<task type="auto">
  <name>Task 2: SpotContextLabel and LastRaiserIndicator</name>
  <files>
    src/components/poker/molecules/SpotContextLabel.tsx
    src/components/poker/molecules/LastRaiserIndicator.tsx
  </files>
  <action>
    Create src/components/poker/molecules/SpotContextLabel.tsx:
    - 'use client'
    - Props: { heroPosition: 'IP' | 'OOP'; potType: '3BP' | 'SRP' | '4BP'; heroRole: 'Aggressor' | 'Caller'; street: Street }
    - Renders a text label: "{potType} {heroPosition} {heroRole}"
    - Examples: "3b IP Aggressor", "SRP OOP Caller", "4b IP Aggressor"
    - Style: small text (text-xs), gray-400 color, uppercase tracking-wider
    - Position: above StreetActionPanel in the training layout

    Create src/components/poker/molecules/LastRaiserIndicator.tsx:
    - 'use client'
    - Props: { position: string; isVisible: boolean }
    - Renders a small chip/token with "LRI" label when isVisible=true
    - Style: small badge, orange background (bg-orange-500), white text, rounded-full, text-xs
    - Purpose: indicates which player was last to raise (the "Last Raiser Indicator" — POST-06 requirement)
    - When isVisible=false, return null
  </action>
  <verify>npx tsc --noEmit exits 0</verify>
  <done>SpotContextLabel renders correct label for all potType/position/role combinations; LastRaiserIndicator renders orange badge or null</done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit exits 0
2. No import errors — all imports resolve correctly
3. Components follow 'use client' pattern like existing molecules
</verification>

<success_criteria>
- PostflopActionButton: pre-decision clickable, post-decision shows frequency + EV, isDeviated shows N/A
- StreetActionPanel: renders all actions, empty actions shows loading state
- SpotContextLabel: renders "3b IP Aggressor" style text
- LastRaiserIndicator: renders orange badge or null
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-postflop-training/10-04-SUMMARY.md`
</output>
