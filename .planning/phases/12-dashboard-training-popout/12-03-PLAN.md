---
phase: 12-dashboard-training-popout
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - src/components/AppHeader.tsx
  - src/app/lobby/page.tsx
  - src/app/session/[id]/page.tsx
  - src/app/summary/[id]/page.tsx
  - src/app/stats/page.tsx
  - src/app/stats/components/WeaknessBreakdown.tsx
  - src/app/signup/page.tsx
  - src/app/login/page.tsx
  - src/app/review/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "All /lobby links point to / or /training as appropriate"
    - "/lobby redirects to / for backward compatibility"
    - "AppHeader shows Dashboard and Training nav links"
    - "AppHeader hides on /training page during active sessions"
  artifacts:
    - path: "src/components/AppHeader.tsx"
      provides: "Updated nav with Dashboard and Training links"
      contains: "Dashboard"
    - path: "src/app/lobby/page.tsx"
      provides: "Backward-compatible redirect to /"
      contains: "redirect"
  key_links:
    - from: "src/components/AppHeader.tsx"
      to: "/"
      via: "nav link"
      pattern: "href.*[\"']/[\"']"
    - from: "src/app/lobby/page.tsx"
      to: "/"
      via: "redirect call"
      pattern: "redirect\\([\"']/[\"']\\)"
    - from: "src/app/stats/components/WeaknessBreakdown.tsx"
      to: "/training"
      via: "router.push for drill navigation"
      pattern: "router\\.push.*training"
---

<objective>
Update all `/lobby` references across the codebase and restructure AppHeader navigation.

Purpose: Completes the routing restructure by pointing all existing `/lobby` references to the new dashboard (`/`) or training page (`/training`), adds backward compatibility redirect, and updates the AppHeader with proper navigation links.

Output: All navigation flows updated, no broken links, `/lobby` remains functional via redirect.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-dashboard-training-popout/12-RESEARCH.md
@.planning/phases/12-dashboard-training-popout/12-01-SUMMARY.md
@.planning/phases/12-dashboard-training-popout/12-02-SUMMARY.md

Files to modify:
@src/components/AppHeader.tsx
@src/app/lobby/page.tsx
@src/app/session/[id]/page.tsx
@src/app/summary/[id]/page.tsx
@src/app/stats/page.tsx
@src/app/stats/components/WeaknessBreakdown.tsx
@src/app/signup/page.tsx
@src/app/login/page.tsx
@src/app/review/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AppHeader navigation</name>
  <files>src/components/AppHeader.tsx</files>
  <action>
Update `src/components/AppHeader.tsx`:

1. Change NAV_LINKS array:
```typescript
const NAV_LINKS = [
  { href: '/', label: 'Dashboard' },
  { href: '/training', label: 'Training' },
  { href: '/stats', label: 'Stats' },
] as const;
```

2. Change the logo/brand Link href from `/lobby` to `/`:
```jsx
<Link href="/" className="text-lg font-bold text-stone-900 dark:text-stone-100">
  EV Trainer
</Link>
```

3. Update the header hide condition to also hide on `/training`:
```typescript
if (pathname.startsWith('/session/') || pathname.startsWith('/training')) return null;
```
This hides the header on the training page since it has its own info bar (matching how `/session/` pages work).

4. Fix `isActive` check for root path `/`:
```typescript
const isActive = href === '/'
  ? pathname === '/'
  : pathname === href || pathname.startsWith(href + '/');
```
Without this fix, `/` would incorrectly match as active for every path.
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | head -10` to verify TypeScript compiles. Check that AppHeader renders "Dashboard", "Training", "Stats" links.
  </verify>
  <done>
AppHeader shows Dashboard (/) + Training (/training) + Stats (/stats) links, hides on /training and /session/ pages, logo links to /.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all /lobby references + backward compat redirect</name>
  <files>
    src/app/lobby/page.tsx
    src/app/session/[id]/page.tsx
    src/app/summary/[id]/page.tsx
    src/app/stats/page.tsx
    src/app/stats/components/WeaknessBreakdown.tsx
    src/app/signup/page.tsx
    src/app/login/page.tsx
    src/app/review/[id]/page.tsx
  </files>
  <action>
Update each file with the following specific changes:

**src/app/lobby/page.tsx** -- Convert to redirect for backward compatibility:
```typescript
import { redirect } from "next/navigation";

export default function LobbyPage() {
  redirect("/");
}
```
Remove the `'use client'` directive and TrainerLobby import (it's now a server component that just redirects). The TrainerLobby component itself is NOT deleted -- it remains for reference, but the lobby page no longer renders it.

**src/app/session/[id]/page.tsx** -- Two changes:
1. Line 648: Change `<Link href="/lobby"` to `<Link href="/"`
2. Line 696: Change `router.push("/lobby")` to `router.push("/")`

**src/app/summary/[id]/page.tsx** -- Two changes:
1. Line 136: Change `<Link href="/lobby"` to `<Link href="/"`
   Also change the link text from "Back to lobby" to "Back to dashboard"
2. Line 172: Change `href="/lobby"` to `href="/training"`
   This "New session" link should go to training page, not dashboard

**src/app/stats/page.tsx** -- One change:
1. Line 44: Change `href="/lobby"` to `href="/"`
   (This is the back link in the stats page header)

**src/app/stats/components/WeaknessBreakdown.tsx** -- One change:
1. Line 96: Change `router.push('/lobby?${params.toString()}')` to `router.push('/training?${params.toString()}')`
   (Drill buttons should navigate to training page with filters)

**src/app/signup/page.tsx** -- One change:
1. Line 50: Change `router.push('/lobby')` to `router.push('/')`

**src/app/login/page.tsx** -- One change:
1. Line 10: Change the default redirect from `'/lobby'` to `'/'`:
   `const redirect = searchParams.get('redirect') ?? '/';`

**src/app/review/[id]/page.tsx** -- One change:
1. Line 79: Change `<Link href="/lobby"` to `<Link href="/"`
   Also change text from "Back to lobby" to "Back to dashboard" (if present)
  </action>
  <verify>
Run `npx next build 2>&1 | tail -20` to verify all files compile.
Run a grep to confirm no remaining `/lobby` references (except the lobby/page.tsx redirect itself and the WeaknessBreakdown comment):
```bash
grep -rn '"/lobby' src/ --include="*.tsx" --include="*.ts" | grep -v 'lobby/page.tsx' | grep -v 'comment'
```
  </verify>
  <done>
All /lobby hrefs and router.push calls updated to / or /training. /lobby page redirects to / for backward compatibility. No stale /lobby references remain in navigation code.
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -rn '"/lobby' src/ --include="*.tsx"` returns only lobby/page.tsx (the redirect)
- AppHeader shows Dashboard + Training + Stats
- Navigating to /lobby redirects to /
- Summary page "New session" links to /training
- WeaknessBreakdown drill button navigates to /training with params
</verification>

<success_criteria>
- Zero stale /lobby references in navigation code (only the redirect page itself)
- AppHeader displays Dashboard, Training, Stats links
- AppHeader hides on /training and /session/ pages
- /lobby backward compat redirect works
- All "back" links point to / (dashboard)
- All "new session" / "drill" links point to /training
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-training-popout/12-03-SUMMARY.md`
</output>
