---
phase: 12-dashboard-training-popout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/config/TrainingConfigDialog.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard at / shows a training card and drill suggestions"
    - "Config dialog opens as centered popout with semi-transparent backdrop"
    - "Config dialog can be opened/closed programmatically via isOpen prop"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard home page with training card and drill suggestions"
      min_lines: 40
    - path: "src/components/config/TrainingConfigDialog.tsx"
      provides: "Native dialog popout for training configuration"
      min_lines: 80
      exports: ["TrainingConfigDialog"]
  key_links:
    - from: "src/app/page.tsx"
      to: "/training"
      via: "Link component on training card"
      pattern: "href.*(/training|training)"
    - from: "src/components/config/TrainingConfigDialog.tsx"
      to: "RangeGridModal pattern"
      via: "native dialog element with showModal/close"
      pattern: "dialogRef\\.current\\.showModal"
---

<objective>
Create the dashboard home page at `/` and the TrainingConfigDialog component.

Purpose: The dashboard replaces the redirect-to-lobby pattern, becoming the app's landing page. The TrainingConfigDialog extracts training config UI into a native `<dialog>` popout that will overlay the poker table on the `/training` page.

Output: Dashboard page at `/` with training card + drill suggestions. Reusable TrainingConfigDialog component following the existing RangeGridModal native dialog pattern.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-dashboard-training-popout/12-RESEARCH.md

Key source files:
@src/app/page.tsx
@src/components/config/TrainerLobby.tsx
@src/components/range/RangeGridModal.tsx
@src/components/config/ConfigCard.tsx
@src/components/config/DrillSuggestions.tsx
@src/components/config/ModeToggle.tsx
@src/components/config/GameSetup.tsx
@src/components/config/PositionFilters.tsx
@src/components/config/PotTypeFilters.tsx
@src/lib/v2/hooks/useTrainerConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard home page at /</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the current redirect-to-lobby in `src/app/page.tsx` with a proper dashboard page.

This must be a `'use client'` component (DrillSuggestions uses client hooks).

Dashboard layout (from TrainerLobby pattern but simplified):
- Page title: "EV Trainer" as h1, subtitle "Dashboard" in text-stone-400
- A training card using ConfigCard (or a simple styled div) with:
  - Title "Start Training"
  - Brief description "Practice preflop decisions with solver feedback"
  - A Link to `/training` styled as a primary button (same style as the "Start Training" button in TrainerLobby: `rounded bg-stone-900 px-6 py-3 font-semibold text-white ... dark:bg-stone-100 dark:text-stone-900`)
- DrillSuggestions component below the training card
  - Import DrillSuggestions from `@/components/config/DrillSuggestions`
  - For onSelectDrill callback, navigate to `/training?heroPosition={drill.positions[0]}` (pass drill filters as URL params so the training page can pre-apply them)
  - Wrap in a ConfigCard with title "Drill Suggestions" subtitle "Your weakest spots"

Layout: `mx-auto max-w-4xl space-y-6 p-6` (matches TrainerLobby pattern).

Import ConfigCard from `@/components/config/ConfigCard` for consistent card styling.
Import Link from `next/link`.
  </action>
  <verify>
Run `npx next build 2>&1 | head -30` to verify the page compiles without errors. Open browser to `/` and confirm it renders a dashboard (not a redirect to /lobby).
  </verify>
  <done>
Dashboard page renders at `/` with a training card linking to `/training` and drill suggestions section.
  </done>
</task>

<task type="auto">
  <name>Task 2: TrainingConfigDialog component</name>
  <files>src/components/config/TrainingConfigDialog.tsx</files>
  <action>
Create `src/components/config/TrainingConfigDialog.tsx` -- a native `<dialog>` popout for training configuration, following the exact RangeGridModal pattern.

Props interface:
```typescript
interface TrainingConfigDialogProps {
  isOpen: boolean;
  onClose: () => void;
  onStartTraining: () => void;
  isStarting: boolean;
  canStart: boolean;
  error: string | null;
  config: TrainerConfig;
  onConfigChange: (updates: Partial<TrainerConfig>) => void;
  isSessionActive?: boolean; // When true, lock session-level fields (mode, gameType, tableSize, stackDepth)
}
```

Implementation (mirror RangeGridModal pattern exactly):
1. `useRef<HTMLDialogElement>(null)` for dialog ref
2. `useEffect` syncs `isOpen` prop to `dialogRef.current.showModal()` / `dialogRef.current.close()` (same pattern as RangeGridModal lines 72-78)
3. Native close event listener via `useEffect` calling `onClose` (same as RangeGridModal lines 81-91)
4. Backdrop click handler: `e.target === dialogRef.current` triggers `onClose` (same as RangeGridModal lines 94-101)
5. Dialog CSS: `w-[95vw] max-w-lg max-h-[90vh] overflow-y-auto rounded-xl bg-gray-900 text-white p-0 backdrop:bg-black/60` (narrower than RangeGridModal since it's a form, not a grid)

Dialog content (from TrainerLobby but restructured for dialog):
- Header with "Training Setup" title + close button (X) matching RangeGridModal style
- ModeToggle with label "Training Mode"
- GameSetup with gameType, tableSize, stackDepth, villainAlwaysRaise
- Collapsible Advanced Filters using native `<details>/<summary>` (same pattern as TrainerLobby lines 149-186):
  - PositionFilters
  - PotTypeFilters
- When `isSessionActive` is true, wrap ModeToggle and GameSetup in `<fieldset disabled className="opacity-50">` with a note "Locked during session"
- Error message display (if error prop is truthy)
- Start Training button at bottom: disabled when `!canStart || isStarting`, calls `onStartTraining`
- Button label: `isStarting ? 'Starting...' : 'Start Training'`

Imports: ModeToggle, GameSetup, PositionFilters, PotTypeFilters from same config directory. TrainerConfig type from `@/lib/v2/config/types`.

Use `'use client'` directive. Use `useRef`, `useEffect`, `useCallback`, `useState` from React.
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | head -20` to verify TypeScript compiles. Verify the component exports correctly by checking the import resolves.
  </verify>
  <done>
TrainingConfigDialog component exists with native dialog element, follows RangeGridModal pattern, accepts config props, and renders training configuration form with lock support for mid-session reopening.
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `/` renders dashboard content (not redirect to /lobby)
- TrainingConfigDialog TypeScript compiles with correct prop interface
- Dialog follows native `<dialog>` pattern matching RangeGridModal
</verification>

<success_criteria>
- Dashboard page at `/` shows training card with link to `/training` and drill suggestions
- TrainingConfigDialog component exists with native dialog, config form, session lock support
- Both files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-training-popout/12-01-SUMMARY.md`
</output>
