---
phase: 12-dashboard-training-popout
plan: 04
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Dashboard at / shows training card and drill suggestions"
    - "Clicking training card navigates to /training with config popout auto-opened"
    - "Config popout is centered dialog with semi-transparent backdrop, table visible behind"
    - "Starting training closes popout and begins session on same page"
    - "Config popout can be reopened mid-session with locked session-level fields"
    - "All /lobby links updated, /lobby redirects to /"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 12 dashboard and training config popout flow.

Purpose: Confirm all 7 success criteria from the roadmap are met through manual walkthrough of the full user flow.

Output: Verified phase completion or identified gaps for closure.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-dashboard-training-popout/12-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete dashboard and training config popout flow:
1. Dashboard home page at `/` with training card and drill suggestions
2. Training page at `/training` with config dialog popout over poker table
3. Inline training session (no route change on start)
4. Mid-session config reopening with locked fields
5. All navigation links updated from /lobby to / or /training
6. /lobby backward compatibility redirect
  </what-built>
  <how-to-verify>
**Start the dev server:** `npm run dev`

**Test 1: Dashboard (SC-1)**
1. Navigate to `http://localhost:3000/`
2. Confirm: Dashboard page with "EV Trainer" title, training card, and drill suggestions
3. Confirm: Training card has a link/button to start training

**Test 2: Training card navigation (SC-2)**
1. Click the training card link on the dashboard
2. Confirm: Navigates to `/training`
3. Confirm: Config popout dialog auto-opens over the poker table

**Test 3: Config popout appearance (SC-3)**
1. On `/training`, observe the config popout
2. Confirm: Centered dialog with semi-transparent backdrop
3. Confirm: Poker table is visible but dimmed behind the dialog
4. Confirm: Dialog contains mode toggle, game setup, position/pot type filters
5. Confirm: ESC key closes the dialog
6. Confirm: Clicking backdrop closes the dialog

**Test 4: Start training inline (SC-4)**
1. Reopen config dialog if closed (gear icon or reload page)
2. Ensure at least one position and pot type are selected
3. Click "Start Training"
4. Confirm: Dialog closes
5. Confirm: Training session begins on the SAME page (`/training` in URL bar)
6. Confirm: Poker table shows cards, action buttons appear
7. Submit an action (click or press 1/2/3)
8. Confirm: Feedback reveals with EV/frequency
9. Press Space or click "Next Hand"

**Test 5: Mid-session config reopen (SC-5)**
1. During active training, click the gear icon in the info bar
2. Confirm: Config dialog reopens
3. Confirm: Mode, Game Type, Table Size, Stack Depth are locked/disabled (dimmed, not clickable)
4. Confirm: Position and Pot Type filters are still editable
5. Close the dialog
6. Confirm: Training resumes normally

**Test 6: /lobby links updated (SC-6)**
1. Navigate to `/stats` -- confirm back link goes to `/`
2. Navigate to a session summary page -- confirm "Back to dashboard" links to `/`
3. Confirm "New session" links to `/training`

**Test 7: /lobby backward compat (SC-7)**
1. Navigate to `http://localhost:3000/lobby`
2. Confirm: Redirects to `/` (dashboard)

**Test 8: AppHeader navigation**
1. Navigate to `/` (dashboard) -- confirm header shows Dashboard, Training, Stats links
2. Navigate to `/training` -- confirm header is hidden
3. Navigate to `/stats` -- confirm header is visible
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and what behavior was observed</resume-signal>
</task>

</tasks>

<verification>
All 7 roadmap success criteria verified through manual walkthrough.
</verification>

<success_criteria>
All 8 tests pass (mapping to 7 roadmap success criteria plus AppHeader verification).
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-training-popout/12-04-SUMMARY.md`
</output>
