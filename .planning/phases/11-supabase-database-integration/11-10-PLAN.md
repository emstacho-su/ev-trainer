---
phase: 11-supabase-database-integration
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedSpots.ts
  - .env.local.example
  - src/app/auth/callback/route.ts
  - src/app/login/page.tsx
  - src/app/signup/page.tsx
autonomous: false
gap_closure: true
user_setup:
  - service: supabase
    why: "Service role key needed for seed script to bypass RLS"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (secret)"
    dashboard_config:
      - task: "Configure OAuth redirect URL"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs -> add http://localhost:3000/auth/callback"
      - task: "Enable Google OAuth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google -> toggle on, add client ID and secret from Google Cloud Console"
      - task: "Enable GitHub OAuth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> GitHub -> toggle on, add client ID and secret from GitHub Developer Settings"

must_haves:
  truths:
    - "npm run seed:spots succeeds when SUPABASE_SERVICE_ROLE_KEY is configured"
    - "OAuth login buttons redirect to provider and complete the auth flow back to the app"
  artifacts:
    - path: ".env.local.example"
      provides: "Template showing all required env vars including SUPABASE_SERVICE_ROLE_KEY"
    - path: "scripts/seedSpots.ts"
      provides: "Seed script with clear error messages and dotenv loading"
    - path: "src/app/auth/callback/route.ts"
      provides: "OAuth PKCE callback handler"
  key_links:
    - from: "scripts/seedSpots.ts"
      to: "supabase.from('spots').upsert"
      via: "service role client bypasses RLS"
      pattern: "createClient.*serviceRoleKey"
    - from: "src/app/login/page.tsx"
      to: "/auth/callback"
      via: "signInWithOAuth redirectTo"
      pattern: "redirectTo.*auth/callback"
---

<objective>
Fix OAuth login flow (G1) and seed script service role key configuration (G2).

Purpose: OAuth login buttons currently fail to complete the auth flow because Supabase Dashboard OAuth providers and redirect URLs are not configured. The seed script fails because SUPABASE_SERVICE_ROLE_KEY is not in .env.local.

Output: Working OAuth login, documented env vars, seed script that runs successfully when key is provided.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-supabase-database-integration/11-VERIFICATION.md

@src/app/auth/callback/route.ts
@src/app/login/page.tsx
@src/app/signup/page.tsx
@scripts/seedSpots.ts
@src/lib/supabase/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix seed script env setup and create .env.local.example</name>
  <files>.env.local.example, scripts/seedSpots.ts</files>
  <action>
1. Create `.env.local.example` file with ALL required env vars for the project:
   ```
   # Supabase (required)
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJ...your-anon-key

   # Supabase Service Role (required for seed script only - KEEP SECRET)
   SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

   # Express Solver (optional - only needed for postflop solver route)
   SOLVER_PORT=4000
   ```

2. Update `scripts/seedSpots.ts` to improve error messaging:
   - After the dotenv.config calls, add a check: if SUPABASE_SERVICE_ROLE_KEY is not set, print a clear message telling the user to copy it from Supabase Dashboard -> Settings -> API -> service_role key, and to add it to .env.local.
   - Ensure dotenv loads .env.local FIRST (it already does, just verify the path resolution works on Windows).

3. Add `seed:spots` script to package.json if not already there:
   - Should be: `"seed:spots": "tsx scripts/seedSpots.ts"`
  </action>
  <verify>
Run `cat .env.local.example` to confirm the file exists with all vars documented.
Run `grep seed:spots package.json` to confirm the npm script exists.
  </verify>
  <done>.env.local.example exists with SUPABASE_SERVICE_ROLE_KEY documented. Seed script prints clear instructions when the key is missing.</done>
</task>

<task type="auto">
  <name>Task 2: Harden OAuth callback and add error diagnostics</name>
  <files>src/app/auth/callback/route.ts, src/app/login/page.tsx, src/app/signup/page.tsx</files>
  <action>
1. Update `src/app/auth/callback/route.ts`:
   - Add logging: `console.log('[auth/callback] code:', code ? 'present' : 'missing')` and `console.error('[auth/callback] exchange error:', error)` for debugging.
   - Pass more specific error info to the redirect: instead of just `?error=auth_callback_failed`, include `?error=auth_callback_failed&message=${encodeURIComponent(error?.message || 'unknown')}` so the login page can display what went wrong.

2. Update `src/app/login/page.tsx` OAuthButtons:
   - In `handleOAuth`, log errors: `console.error('[OAuth] signInWithOAuth error:', error)`.
   - Update the error param parsing to also show the `message` query param if present, so users see specifically why OAuth failed.
   - Ensure the `redirectTo` URL is correct. The current code uses `window.location.origin + '/auth/callback'`. This is correct IF the Supabase Dashboard has `http://localhost:3000/auth/callback` (or the deployed URL) in its Redirect URLs list. Add a console.log of the redirectTo URL so users can verify it matches their Supabase config.

3. Update `src/app/signup/page.tsx` similarly:
   - If it has OAuth buttons, apply the same error logging and redirectTo logging.
   - Check if it has an OAuthButtons component or inline OAuth -- apply same fixes.

Note: The actual OAuth configuration (enabling providers, adding redirect URLs) must be done in the Supabase Dashboard by the user. Claude cannot do this. The code changes here ensure that when OAuth IS configured, it works, and when it's NOT configured, the error messages are helpful enough for the user to diagnose the issue.
  </action>
  <verify>
Run `npx next build` -- build should succeed.
Grep for `console.error.*auth/callback` in the callback route to confirm logging is added.
  </verify>
  <done>OAuth callback has error logging and passes error details to login page. Login/signup pages display specific OAuth error messages. User can diagnose Supabase Dashboard configuration issues from the error output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>OAuth flow diagnostics and seed script env configuration. The code changes ensure better error messages. The actual fix requires Supabase Dashboard configuration.</what-built>
  <how-to-verify>
1. Check that `.env.local.example` exists and documents SUPABASE_SERVICE_ROLE_KEY
2. Copy SUPABASE_SERVICE_ROLE_KEY from Supabase Dashboard -> Settings -> API into your .env.local
3. Run `npm run seed:spots` -- should succeed and seed spots to Supabase
4. In Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs, add: `http://localhost:3000/auth/callback`
5. In Supabase Dashboard -> Authentication -> Providers, enable Google and/or GitHub with their OAuth credentials
6. Try clicking "Continue with Google" or "Continue with GitHub" on the login page -- should redirect and complete login
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- .env.local.example documents all required env vars
- Seed script runs successfully with SUPABASE_SERVICE_ROLE_KEY configured
- OAuth callback logs errors with useful details
- Login page shows specific error messages for OAuth failures
- Build passes with no errors
</verification>

<success_criteria>
- G1 (OAuth): Error diagnostics added so user can configure Supabase Dashboard correctly. OAuth works when providers and redirect URLs are configured.
- G2 (Service role key): .env.local.example created, seed script has clear error messages. Script runs when key is provided.
</success_criteria>

<output>
After completion, create `.planning/phases/11-supabase-database-integration/11-10-SUMMARY.md`
</output>
