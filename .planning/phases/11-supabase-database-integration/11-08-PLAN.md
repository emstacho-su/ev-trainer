---
phase: 11-supabase-database-integration
plan: 08
type: execute
wave: 5
depends_on: ["11-04", "11-05", "11-06", "11-07"]
files_modified:
  - package.json
  - src/server/index.ts
  - src/server/app.ts
  - src/lib/prisma/client.ts
autonomous: true

must_haves:
  truths:
    - "No Prisma imports remain in active code paths (except solver route)"
    - "No custom JWT/Argon2/jose imports in active code"
    - "Express server only serves solver compute route"
    - "Removed packages no longer in node_modules"
    - "Build passes without removed packages"
  artifacts:
    - path: "package.json"
      provides: "Clean dependency list without removed packages"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "supabase|@supabase"
---

<objective>
Remove Express backend code (except solver route), Prisma, custom auth, and unused packages. Clean up the codebase.

Purpose: With Supabase handling auth, database, and data layer, the old Express/Prisma/custom auth code is dead weight. Removing it reduces bundle size, simplifies the codebase, and prevents confusion about which code path is active.
Output: Clean codebase with only Supabase and solver-related code. ~15 packages removed.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-supabase-database-integration/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Express server to solver-only and remove old auth</name>
  <files>
    src/server/app.ts
    src/server/index.ts
    src/server/routes/postflop.routes.ts
  </files>
  <action>
    1. Simplify `src/server/app.ts`:
       - Keep ONLY: express app creation, CORS middleware, JSON parsing
       - Keep ONLY: postflop solver route (POST /api/postflop/solve)
       - Remove: session routes, stats routes, health routes, auth routes, drill routes, config routes
       - Remove: auth middleware imports, rate limiting (Supabase handles this)
       - Remove: helmet, compression (Next.js handles these for web traffic)
       - The solver route is the ONLY Express endpoint

    2. Simplify `src/server/index.ts`:
       - Keep: Express server startup on port 4000
       - Remove: Prisma connection initialization
       - Remove: setSessionStoreBackend() calls
       - The server's only job is hosting the solver compute route

    3. Preserve `src/server/routes/postflop.routes.ts`:
       - This route calls the CFR+ solver -- it stays on Express because solver is CPU-intensive
       - Remove any auth middleware wrapping (solver doesn't need auth -- it's a compute endpoint)

    4. Delete the following directories/files entirely:
       - `src/server/auth/` (entire directory -- all auth code)
       - `src/server/oauth/` (entire directory -- OAuth code)
       - `src/server/email/` (entire directory -- email service)
       - `src/server/controllers/session.controller.ts` (sessions now in Next.js API routes)
       - `src/server/controllers/stats.controller.ts` (stats now in Next.js API routes)
       - `src/server/controllers/config.controller.ts`
       - `src/server/controllers/drill.controller.ts`
       - `src/server/routes/session.routes.ts`
       - `src/server/routes/stats.routes.ts`
       - `src/server/routes/health.routes.ts`
       - `src/server/routes/config.routes.ts`
       - `src/server/routes/drill.routes.ts`
       - `src/server/middleware/` (entire directory -- error middleware, etc.)

    5. Delete Prisma files:
       - `src/lib/prisma/client.ts` (Prisma client singleton)
       - `src/lib/v2/storage/prismaSessionStore.ts` (Prisma session store backend)
       - `prisma/schema.prisma` (Prisma schema)
       - `prisma/migrations/` (Prisma migrations)
       - `prisma/seed-stats.ts`

    6. Delete old auth-related code in v2:
       - Any files that import from jose, argon2, or the old auth service

    **Do NOT delete:**
    - `src/lib/engine/` (solver core -- unchanged)
    - `src/lib/runtime/` (session registry -- still used for in-memory state during training)
    - `src/lib/v2/` (session handlers, filters, etc. -- still used for business logic)
    - `src/lib/v2/storage/sessionStorage.ts` (localStorage -- still used for guest persistence)
    - `src/lib/v2/packs/` (pack loader -- still used as fallback)
    - `src/server/routes/postflop.routes.ts` (solver route -- stays)
  </action>
  <verify>
    - `npm run build` passes
    - `grep -r "prisma" src/ --include="*.ts" --include="*.tsx" -l` returns no files (except maybe types or comments)
    - `grep -r "argon2\|jose" src/ --include="*.ts" -l` returns no files
    - Express server still starts and serves /api/postflop/solve
    - Deleted directories/files no longer exist
  </verify>
  <done>Express server stripped to solver-only. All Prisma, custom auth, and old controller code deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused packages and clean dependencies</name>
  <files>
    package.json
  </files>
  <action>
    1. Uninstall packages that are no longer needed:
       ```bash
       npm uninstall @prisma/client @prisma/adapter-pg prisma pg argon2 jose resend helmet compression cookie-parser express-rate-limit
       ```

    2. Verify remaining packages are correct:
       - KEEP: express (for solver route), cors (for solver CORS)
       - KEEP: @supabase/supabase-js, @supabase/ssr
       - KEEP: All Next.js, React, Tailwind, Vitest, shadcn/ui, recharts, motion, etc.
       - REMOVE: Everything listed in 11-RESEARCH.md "Packages to REMOVE" section

    3. Also remove from devDependencies:
       ```bash
       npm uninstall @types/pg @types/express @types/cors @types/compression @types/cookie-parser supertest @types/supertest
       ```
       Note: Only remove @types packages for libraries that were fully removed. Keep @types/express and @types/cors since Express and cors are still used for the solver route.

    4. Run `npm install` to update lockfile.

    5. Run `npm run build` to verify nothing breaks.

    6. Run `npm test` to verify existing tests pass (some tests may need updates if they imported deleted modules).
       - Tests that imported from deleted files (auth service tests, Prisma tests) should be deleted along with the source files in Task 1
       - Engine tests should all still pass (engine layer unchanged)

    7. Update .env.example:
       - Remove old env vars: DATABASE_URL, JWT_SECRET, JWT_REFRESH_SECRET, RESEND_API_KEY, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET
       - Keep: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
       - Add: SUPABASE_SERVICE_ROLE_KEY (for seed scripts only, with comment)
  </action>
  <verify>
    - `npm run build` passes
    - `npm test` passes (all remaining tests)
    - package.json does not contain: @prisma/client, @prisma/adapter-pg, prisma, pg, argon2, jose, resend, helmet, compression, cookie-parser, express-rate-limit
    - package.json still contains: express, cors, @supabase/supabase-js, @supabase/ssr
    - .env.example has Supabase vars only
  </verify>
  <done>~15 packages removed. Build passes. Tests pass. Clean dependency tree with Supabase + solver-only Express.</done>
</task>

</tasks>

<verification>
- `npm run build` passes cleanly
- `npm test` passes
- No Prisma, argon2, jose imports in any active source file
- Express server starts and serves only /api/postflop/solve
- Package count reduced by ~15
- .env.example documents only current env vars
</verification>

<success_criteria>
Codebase cleaned of all dead infrastructure code. Only Supabase client and solver Express route remain as external dependencies. Build and tests pass. Package list lean.
</success_criteria>

<output>
After completion, create `.planning/phases/11-supabase-database-integration/11-08-SUMMARY.md`
</output>
