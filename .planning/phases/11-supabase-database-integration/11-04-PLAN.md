---
phase: 11-supabase-database-integration
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - src/app/login/page.tsx
  - src/app/signup/page.tsx
  - src/app/auth/error/page.tsx
  - src/components/AppHeader.tsx
autonomous: true

must_haves:
  truths:
    - "User can sign up with email and password"
    - "User can log in with email and password"
    - "User can log in with Google OAuth"
    - "User can log in with GitHub OAuth"
    - "User can log out from the app header"
    - "Auth state reflected in header (login button vs user info)"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Login page with email/password and OAuth buttons"
      contains: "signInWithPassword|signInWithOAuth"
    - path: "src/app/signup/page.tsx"
      provides: "Signup page with email/password form"
      contains: "signUp"
    - path: "src/components/AppHeader.tsx"
      provides: "Header with auth-aware navigation"
      contains: "useAuth|signOut"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "src/lib/supabase/client.ts"
      via: "createClient for auth operations"
      pattern: "createClient|signInWith"
    - from: "src/components/AppHeader.tsx"
      to: "src/app/providers/AuthProvider.tsx"
      via: "useAuth hook for user state"
      pattern: "useAuth"
---

<objective>
Rebuild the login and signup pages to use Supabase Auth, and update the AppHeader to show auth state with logout.

Purpose: Users need working auth UI to create accounts and log in. The header needs to reflect auth state (show login button vs user info + logout).
Output: Working login/signup pages with email/password + OAuth, auth-aware header.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-supabase-database-integration/11-RESEARCH.md
@src/app/login/page.tsx
@src/app/signup/page.tsx
@src/components/AppHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild login and signup pages with Supabase Auth</name>
  <files>
    src/app/login/page.tsx
    src/app/signup/page.tsx
    src/app/auth/error/page.tsx
  </files>
  <action>
    1. Rebuild `src/app/login/page.tsx`:
       - 'use client' directive
       - Import createClient from `@/lib/supabase/client`
       - State: email, password, error, loading
       - Email/password form:
         * On submit: `supabase.auth.signInWithPassword({ email, password })`
         * On success: `router.push('/')` or redirect param
         * On error: display error message
       - OAuth buttons (Google, GitHub):
         * On click: `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: `${origin}/auth/callback` } })`
         * Same pattern for GitHub
       - Link to signup page: "Don't have an account? Sign up"
       - Dark theme styling consistent with existing app (bg-gray-900, text-white, etc.)
       - Center the form on page, max-w-md, card-style container

    2. Rebuild `src/app/signup/page.tsx`:
       - 'use client' directive
       - State: email, password, confirmPassword, displayName, error, loading, success
       - Email/password form:
         * Validate password match
         * On submit: `supabase.auth.signUp({ email, password, options: { data: { display_name: displayName } } })`
         * No email verification required (per CONTEXT.md) -- account active immediately
         * On success: redirect to '/' (user is logged in)
         * On error: display error message
       - OAuth buttons (same as login -- can reuse component)
       - Link to login page: "Already have an account? Log in"
       - Same dark theme styling as login

    3. Create `src/app/auth/error/page.tsx`:
       - Simple error page for auth failures
       - Display error message from searchParams if present
       - Link back to login page
       - Dark themed

    Style all pages consistently with existing poker theme (dark backgrounds, green accents for primary actions, consistent with globals.css custom properties).
  </action>
  <verify>
    - `npm run build` passes
    - Login page renders with email/password form and OAuth buttons
    - Signup page renders with email/password form, display name field, and OAuth buttons
    - Auth error page renders with message and login link
  </verify>
  <done>Login and signup pages use Supabase Auth for all authentication flows. Email/password and OAuth (Google, GitHub) all route through Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppHeader with auth-aware navigation</name>
  <files>
    src/components/AppHeader.tsx
  </files>
  <action>
    1. Update `src/components/AppHeader.tsx`:
       - Import `useAuth` from `@/app/providers/AuthProvider`
       - Import `createClient` from `@/lib/supabase/client`
       - Get `{ user, loading }` from useAuth()

    2. When user is NOT logged in:
       - Show "Log In" button that navigates to /login
       - Show "Sign Up" button that navigates to /signup (secondary style)

    3. When user IS logged in:
       - Show user email or display name (truncated if long)
       - Show "Log Out" button:
         * On click: `supabase.auth.signOut()` then `router.push('/')`
       - Stats link visible (authenticated feature)

    4. While loading:
       - Show skeleton/placeholder (small gray bar) instead of buttons to prevent flash

    5. Keep ALL existing header functionality (app name, theme toggle if present, navigation links).
       Do NOT remove or break existing nav items. ADD auth-aware elements alongside.

    6. Responsive: on mobile, auth buttons should be compact (icon-only or abbreviated).
  </action>
  <verify>
    - `npm run build` passes
    - AppHeader imports useAuth
    - Header shows login/signup when no user
    - Header shows user info + logout when logged in
    - Existing header navigation preserved
  </verify>
  <done>AppHeader reflects auth state. Shows login/signup for guests, user info + logout for authenticated users. All existing navigation preserved.</done>
</task>

</tasks>

<verification>
- Full auth flow works: signup -> auto-login -> header shows user -> logout -> header shows login
- OAuth buttons trigger Supabase OAuth flow (redirect to provider)
- Error states display properly (wrong password, existing email, etc.)
- `npm run build` passes
</verification>

<success_criteria>
Users can create accounts, log in with email/password or OAuth, see their auth state in the header, and log out. All auth flows use Supabase Auth exclusively.
</success_criteria>

<output>
After completion, create `.planning/phases/11-supabase-database-integration/11-04-SUMMARY.md`
</output>
