---
phase: 11-supabase-database-integration
plan: 09
type: execute
wave: 6
depends_on: ["11-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 5 phase success criteria verified by human testing"
    - "Auth flow works end-to-end (signup, login, OAuth, logout)"
    - "Training sessions persist across browser refresh for authenticated users"
    - "Guest training works without account"
    - "Stats page shows real data from Supabase"
---

<objective>
Human verification checkpoint for the complete Supabase migration.

Purpose: Verify all 5 phase success criteria are met through hands-on testing of the full application flow.
Output: Verified or issues identified for gap closure.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Supabase migration: auth, database, sessions, stats, spots, cleanup</what-built>
  <how-to-verify>
    **Pre-requisites:** `npm run dev` running, Supabase project configured

    **SC-1: Supabase project configured with database schema**
    1. Open Supabase Dashboard -> Database -> Tables
    2. Verify tables exist: profiles, spots, training_sessions, session_entries, daily_stats, spot_stats
    3. Verify RLS enabled on all tables (lock icon visible)

    **SC-2: User authentication via Supabase Auth**
    1. Navigate to /signup
    2. Create account with email/password + display name
    3. Verify redirect to home page and header shows logged-in state
    4. Log out via header button
    5. Log in via /login with same credentials
    6. Test OAuth: click Google login button, complete flow
    7. Verify profile row auto-created in Supabase profiles table

    **SC-3: Practice spot hand database**
    1. Run `npm run seed:spots` to populate spots table
    2. Verify spots appear in Supabase spots table
    3. Start a training session while logged in
    4. Verify spots load (training works)

    **SC-4: Data layer migrated to Supabase**
    1. Complete a full training session (10 decisions) while logged in
    2. Check Supabase dashboard: training_sessions row exists with is_complete = true
    3. Check session_entries: 10 rows with grades
    4. Refresh browser: navigate to session history -- same session appears
    5. Navigate to stats page: data reflects completed session

    **SC-5: Feature parity**
    1. Test guest training: log out, start training, complete session
    2. Guest training should work without database persistence
    3. Flag a hand in session review
    4. Check drill suggestions show on lobby/dashboard
    5. Test postflop training (if Phase 10 complete) -- solver route still works

    **Bonus checks:**
    - Open two browser tabs: verify auth state syncs between tabs
    - Check Supabase Dashboard -> Auth -> Users: user appears
    - Verify no Express routes used except /api/postflop/solve
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass, or describe specific failures</resume-signal>
</task>

</tasks>

<verification>
All 5 phase success criteria from ROADMAP.md verified:
1. Supabase project configured with database schema
2. User account creation and authentication via Supabase Auth
3. Practice spot hand database stores and retrieves training hands
4. Data layer migrated to Supabase client
5. Data layer provides same functionality with Supabase as backend
</verification>

<success_criteria>
Human confirms all features work end-to-end with Supabase backend. No regressions in training, stats, or auth flows.
</success_criteria>

<output>
After completion, create `.planning/phases/11-supabase-database-integration/11-09-SUMMARY.md`
</output>
