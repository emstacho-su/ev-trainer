---
phase: 11-supabase-database-integration
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/app/providers/AuthProvider.tsx
  - src/middleware.ts
  - src/app/layout.tsx
  - src/app/auth/callback/route.ts
  - src/app/auth/confirm/route.ts
autonomous: true

must_haves:
  truths:
    - "Auth state available via useAuth() hook in any client component"
    - "Middleware refreshes Supabase auth tokens on every request"
    - "OAuth callback exchanges code for session via PKCE"
    - "Unauthenticated users redirected from protected routes"
  artifacts:
    - path: "src/app/providers/AuthProvider.tsx"
      provides: "React context with user state from onAuthStateChange"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/middleware.ts"
      provides: "Token refresh and route protection"
      contains: "updateSession"
    - path: "src/app/auth/callback/route.ts"
      provides: "OAuth PKCE code exchange"
      exports: ["GET"]
  key_links:
    - from: "src/app/providers/AuthProvider.tsx"
      to: "src/lib/supabase/client.ts"
      via: "createClient() for browser auth listener"
      pattern: "createClient|onAuthStateChange"
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "updateSession call"
      pattern: "updateSession"
    - from: "src/app/auth/callback/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "exchangeCodeForSession"
      pattern: "exchangeCodeForSession"
---

<objective>
Integrate Supabase Auth into the Next.js app: AuthProvider context, middleware for token refresh, and OAuth callback route.

Purpose: This replaces the custom JWT/Argon2 auth system (Phase 3) with Supabase Auth. Every authenticated feature depends on this working.
Output: Working auth state management, middleware token refresh, OAuth callback handler.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-supabase-database-integration/11-RESEARCH.md
@src/app/layout.tsx
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthProvider and wire into app layout</name>
  <files>
    src/app/providers/AuthProvider.tsx
    src/app/layout.tsx
  </files>
  <action>
    1. Create `src/app/providers/AuthProvider.tsx`:
       - 'use client' directive
       - Import `createClient` from `@/lib/supabase/client`
       - Import `User` type from `@supabase/supabase-js`
       - Create AuthContext with `{ user: User | null; loading: boolean }` shape
       - AuthProvider component:
         * useState for user (null) and loading (true)
         * Create supabase client via createClient()
         * useEffect with onAuthStateChange listener:
           - On any event, set user to session?.user ?? null, set loading to false
         * Return cleanup: subscription.unsubscribe()
       - Export `useAuth` hook: `useContext(AuthContext)`

    2. Update `src/app/layout.tsx`:
       - Import AuthProvider from `@/app/providers/AuthProvider`
       - Wrap children with AuthProvider INSIDE ThemeProvider (auth needs to be available to all components)
       - Order: ThemeProvider > AnimationProvider > AuthProvider > ToastProvider > AppHeader + children
       - Keep all existing providers (ThemeProvider, AnimationProvider, ToastProvider)

    Do NOT remove any existing providers or change their order relative to each other. AuthProvider slots in as a new layer.
  </action>
  <verify>
    - `npm run build` passes
    - AuthProvider.tsx exports AuthProvider and useAuth
    - layout.tsx includes AuthProvider wrapping children
    - No existing functionality broken (ThemeProvider, AnimationProvider, ToastProvider all still present)
  </verify>
  <done>AuthProvider provides user state via useAuth() hook. Auth state updates automatically on login/logout/token refresh via onAuthStateChange listener.</done>
</task>

<task type="auto">
  <name>Task 2: Replace middleware and create OAuth callback route</name>
  <files>
    src/middleware.ts
    src/app/auth/callback/route.ts
    src/app/auth/confirm/route.ts
  </files>
  <action>
    1. Replace `src/middleware.ts` entirely:
       - Import `updateSession` from `@/lib/supabase/middleware`
       - Export async middleware function that calls updateSession(request)
       - Export config with matcher that excludes static files and images:
         ```
         matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)']
         ```
       - The updateSession function (from Plan 01) already handles:
         * Creating server client with request cookies
         * Calling getUser() to refresh tokens
         * Propagating cookies to response
       - Add redirect logic to updateSession in `src/lib/supabase/middleware.ts`:
         * If no user AND path requires auth (starts with /stats, /review, /summary), redirect to /login
         * Allow access to: /, /auth/*, /login, /signup, /lobby, /session/*, /train, /preflop-training, /postflop-training, /table-ui-demo
         * Guest users can still train -- do NOT block training routes

    2. Create `src/app/auth/callback/route.ts`:
       - Export GET handler
       - Extract `code` and `next` from searchParams
       - If code present: create server client, call `exchangeCodeForSession(code)`
       - On success: redirect to `next` param or '/'
       - On error: redirect to '/auth/error' or '/login?error=auth_callback_failed'

    3. Create `src/app/auth/confirm/route.ts`:
       - Export GET handler for email confirmation (magic link / password reset)
       - Extract `token_hash` and `type` from searchParams
       - Call `supabase.auth.verifyOtp({ token_hash, type })`
       - On success: redirect to '/'
       - On error: redirect to '/login?error=confirmation_failed'
  </action>
  <verify>
    - `npm run build` passes
    - middleware.ts uses Supabase updateSession (not old custom token check)
    - /auth/callback route handler exists
    - /auth/confirm route handler exists
    - Protected routes still redirect unauthenticated users
    - Training routes remain accessible to guests
  </verify>
  <done>Middleware refreshes Supabase tokens on every request. OAuth callback exchanges PKCE code for session. Email confirmation route handles magic links. Protected routes redirect unauthenticated users while training routes remain open to guests.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- useAuth() returns { user, loading } in any client component
- Middleware runs on all routes (except static assets)
- OAuth callback route at /auth/callback
- Training pages accessible without login
- Stats/review pages redirect to login without auth
</verification>

<success_criteria>
Supabase Auth fully integrated into Next.js app. Auth state available everywhere via useAuth(). Middleware handles token refresh. OAuth callback ready for Google/GitHub login.
</success_criteria>

<output>
After completion, create `.planning/phases/11-supabase-database-integration/11-03-SUMMARY.md`
</output>
