---
phase: 06-trainer-configuration
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/app/lobby/page.tsx
  - src/components/config/TrainerLobby.tsx
autonomous: true

must_haves:
  truths:
    - "User sees lobby screen before training session"
    - "User can configure mode, game setup, and filters on lobby"
    - "User can expand/collapse Advanced Filters section"
    - "User cannot click Start Training with invalid filter state"
    - "Lobby remembers last used configuration from localStorage"
  artifacts:
    - path: "src/app/lobby/page.tsx"
      provides: "Lobby route"
      min_lines: 10
    - path: "src/components/config/TrainerLobby.tsx"
      provides: "Lobby screen with config cards and Start Training button"
      min_lines: 80
  key_links:
    - from: "src/components/config/TrainerLobby.tsx"
      to: "src/lib/v2/hooks/useTrainerConfig.ts"
      via: "hook provides config state"
      pattern: "useTrainerConfig\\("
    - from: "src/components/config/TrainerLobby.tsx"
      to: "src/components/config/PositionFilters.tsx"
      via: "renders filter components"
      pattern: "<PositionFilters"
    - from: "src/components/config/TrainerLobby.tsx"
      to: "/session"
      via: "navigates to session page on Start Training"
      pattern: "router\\.push.*session"
---

<objective>
Lobby screen with card-based configuration layout, filter components, and Start Training button that validates config before starting session.

Purpose: Provide dedicated pre-session configuration screen where users set up training parameters and review settings before entering table.

Output: Functional lobby page at /lobby with essentials card, advanced filters (collapsible), and validated session start.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\estac\projects\ev-trainer\.planning\PROJECT.md
@C:\Users\estac\projects\ev-trainer\.planning\ROADMAP.md
@C:\Users\estac\projects\ev-trainer\.planning\STATE.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-CONTEXT.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-RESEARCH.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-01-PLAN.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create lobby page route</name>
  <files>
    src/app/lobby/page.tsx
  </files>
  <action>
    Create Next.js App Router page at /lobby.

    Import TrainerLobby component.

    Render TrainerLobby as main content.

    Export as default (follows Next.js App Router conventions).

    Use 'use client' directive (interactive page with hooks).

    Include page metadata (export metadata with title: "Trainer Lobby").
  </action>
  <verify>
    Navigate to http://localhost:3000/lobby shows lobby page
  </verify>
  <done>
    Lobby route exists at /lobby and renders TrainerLobby component
  </done>
</task>

<task type="auto">
  <name>Create TrainerLobby component with config cards and session start</name>
  <files>
    src/components/config/TrainerLobby.tsx
  </files>
  <action>
    Create TrainerLobby component with comprehensive lobby layout.

    Hooks:
    - useTrainerConfig() to load/save config from Plan 06-01
    - useRouter() from next/navigation for navigation
    - useState for expandable Advanced Filters section (default collapsed)
    - useMemo to compute canStart (positions.length > 0 && potTypes.length > 0)

    Import filter components from Plan 06-02:
    - ConfigCard, ModeToggle, GameSetup, PositionFilters, PotTypeFilters

    Layout structure:
    - Header: h1 "Trainer Configuration", subtitle "Customize your training session"
    - Essentials Card (always visible):
      - ModeToggle (Preflop/Flop)
      - GameSetup (game type, table size, stack depth)
    - Advanced Filters (collapsible details/summary element):
      - Summary text "Advanced Filters" with expand/collapse arrow (▶ / ▼)
      - Grid with two cards when expanded:
        - PositionFilters card
        - PotTypeFilters card
    - Start Training button section:
      - Validation message (red text) if !canStart: "Select at least one position and pot type"
      - Button disabled={!canStart}
      - onClick: create session via POST /api/session/start with config, then router.push('/session')

    Styling:
    - Max width: max-w-4xl mx-auto
    - Vertical spacing: space-y-6
    - Grid for cards: grid gap-4 lg:grid-cols-2
    - Button: rounded bg-stone-900 px-6 py-3 font-semibold text-white disabled:opacity-50

    Session start logic:
    - POST to /api/session/start with body: { seed: crypto.randomUUID(), mode: config.mode, packId: 'default', filters: { positions: config.positions, potTypes: config.potTypes } }
    - On success (201), navigate to /session?sessionId={responseSessionId}
    - On error, show error toast (use console.error for now, toast integration comes later)

    Use 'use client' directive. Export as default.

    Include JSDoc explaining lobby is pre-session config screen, user confirms settings before entering table.
  </action>
  <verify>
    Lobby renders with Essentials card visible, Advanced Filters collapsible, Start Training button validates filters, clicking Start navigates to /session
  </verify>
  <done>
    TrainerLobby component renders config cards, enforces validation, starts session and navigates to /session on valid config
  </done>
</task>

</tasks>

<verification>
- Navigate to /lobby shows lobby page
- Lobby loads last used config from localStorage on mount
- ModeToggle, GameSetup, PositionFilters, PotTypeFilters all functional
- Advanced Filters section expands/collapses
- Start Training button disabled when filters invalid
- Validation message shown when filters invalid
- Clicking Start Training with valid config creates session and navigates to /session
</verification>

<success_criteria>
- Lobby page exists at /lobby route
- TrainerLobby component uses useTrainerConfig for state persistence
- Essentials card shows mode toggle and game setup dropdowns
- Advanced Filters section collapses/expands with position and pot type filters
- Start Training button validates config before starting session
- Session starts via POST /api/session/start and navigates to /session on success
</success_criteria>

<output>
After completion, create `.planning/phases/06-trainer-configuration/06-03-SUMMARY.md`
</output>
