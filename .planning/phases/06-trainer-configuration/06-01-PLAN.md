---
phase: 06-trainer-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/v2/config/types.ts
  - src/lib/v2/config/validation.ts
  - src/lib/v2/config/configStore.ts
  - src/lib/v2/hooks/useTrainerConfig.ts
  - prisma/schema.prisma
  - src/server/routes/config.routes.ts
  - src/server/controllers/config.controller.ts
autonomous: true

must_haves:
  truths:
    - "Config validation prevents empty position/pot type selections"
    - "localStorage remembers last lobby configuration across browser sessions"
    - "Authenticated users can sync config to database"
    - "Config schema validates on load and fills missing defaults"
  artifacts:
    - path: "src/lib/v2/config/types.ts"
      provides: "TrainerConfig type and filter types"
      exports: ["TrainerConfig", "PositionFilter", "PotTypeFilter"]
    - path: "src/lib/v2/config/validation.ts"
      provides: "Zod schemas for config validation"
      exports: ["trainerConfigSchema", "validateConfig"]
    - path: "src/lib/v2/config/configStore.ts"
      provides: "localStorage persistence with Zod validation"
      exports: ["loadConfigFromStorage", "saveConfigToStorage"]
    - path: "src/lib/v2/hooks/useTrainerConfig.ts"
      provides: "Custom hook for config state + persistence"
      exports: ["useTrainerConfig"]
    - path: "prisma/schema.prisma"
      provides: "User.trainerPreferences JSON field"
      contains: "trainerPreferences"
  key_links:
    - from: "src/lib/v2/hooks/useTrainerConfig.ts"
      to: "src/lib/v2/config/configStore.ts"
      via: "loadConfigFromStorage on mount"
      pattern: "loadConfigFromStorage\\(\\)"
    - from: "src/lib/v2/hooks/useTrainerConfig.ts"
      to: "/api/config"
      via: "fetch to sync authenticated user config"
      pattern: "fetch.*api/config"
    - from: "src/lib/v2/config/configStore.ts"
      to: "src/lib/v2/config/validation.ts"
      via: "trainerConfigSchema.parse on deserialize"
      pattern: "trainerConfigSchema\\.parse"
---

<objective>
Configuration foundation: types, validation schemas, localStorage persistence, and server-side sync for authenticated users.

Purpose: Provide type-safe config state management with client-side persistence (localStorage) and optional server-side sync (database) for cross-device consistency.

Output: Config layer that supports both lobby and sidebar configuration surfaces with validation and persistence.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\estac\projects\ev-trainer\.planning\PROJECT.md
@C:\Users\estac\projects\ev-trainer\.planning\ROADMAP.md
@C:\Users\estac\projects\ev-trainer\.planning\STATE.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-CONTEXT.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-RESEARCH.md
@C:\Users\estac\projects\ev-trainer\src\lib\v2\filters\spotFilters.ts
@C:\Users\estac\projects\ev-trainer\prisma\schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create config types and validation schemas</name>
  <files>
    src/lib/v2/config/types.ts
    src/lib/v2/config/validation.ts
  </files>
  <action>
    Create TrainerConfig TypeScript interface with fields:
    - mode: 'PREFLOP' | 'FLOP' (lobby-only, not changeable mid-session)
    - gameType: 'CASH' | 'HU' (lobby-only)
    - tableSize: '6max' | '9max' (lobby-only)
    - stackDepth: '50bb' | '100bb' | '200bb' (lobby-only)
    - villainAlwaysRaise: boolean (lobby-only, default false — forces villain to always raise for aggressive practice)
    - positions: string[] (multi-select, min 1 required, changeable mid-session)
    - potTypes: string[] (multi-select, min 1 required, changeable mid-session)
    - handCountTarget?: number (optional, changeable mid-session)

    Create Zod schema trainerConfigSchema with validation:
    - positions array minimum length 1
    - potTypes array minimum length 1
    - handCountTarget optional, min 1, max 1000 if provided
    - villainAlwaysRaise default false
    - Default values: mode='PREFLOP', gameType='CASH', tableSize='6max', stackDepth='100bb', villainAlwaysRaise=false, positions=['BB','SB'], potTypes=['SRP']

    Export type aliases for Position (UTG | HJ | CO | BTN | SB | BB) and PotType ('SRP' | '3BP' | '4BP').

    Include JSDoc comments explaining lobby-only vs mid-session-changeable fields.
  </action>
  <verify>
    npm run typecheck passes with no errors in config/ directory
  </verify>
  <done>
    TrainerConfig type exported with all required fields, Zod schema validates and provides defaults, type aliases for Position and PotType exist
  </done>
</task>

<task type="auto">
  <name>Implement localStorage persistence with Zod validation</name>
  <files>
    src/lib/v2/config/configStore.ts
  </files>
  <action>
    Create localStorage persistence layer with two functions:

    loadConfigFromStorage():
    - Read 'trainer-config' key from localStorage
    - If not found, return default config from schema
    - Parse JSON, validate with trainerConfigSchema.parse()
    - If validation fails (schema change, corrupt data), log error and return default
    - Return validated TrainerConfig

    saveConfigToStorage(config: TrainerConfig):
    - Validate config with trainerConfigSchema.parse() before saving
    - Serialize with JSON.stringify()
    - Save to localStorage with key 'trainer-config'
    - Handle localStorage quota errors gracefully (log but don't throw)

    Use try-catch blocks for localStorage access (might be disabled, quota exceeded, or unavailable in SSR).

    Include comment explaining this handles browser-side persistence; server-side sync is separate.
  </action>
  <verify>
    Unit tests verify: loadConfigFromStorage returns defaults when empty, saveConfigToStorage round-trips data correctly, validation failures return defaults
  </verify>
  <done>
    loadConfigFromStorage and saveConfigToStorage handle localStorage with Zod validation, corrupt data falls back to defaults gracefully
  </done>
</task>

<task type="auto">
  <name>Create useTrainerConfig hook with auto-save and server sync</name>
  <files>
    src/lib/v2/hooks/useTrainerConfig.ts
  </files>
  <action>
    Create custom React hook useTrainerConfig(userId?: string):

    State management:
    - useState for config (TrainerConfig | null)
    - useState for isLoading (boolean, true on mount)

    On mount effect:
    - Load from localStorage via loadConfigFromStorage()
    - Set config state, set isLoading to false
    - If userId provided, fetch from /api/config and merge with localStorage (server takes precedence)

    On config change effect (debounced 500ms):
    - Save to localStorage via saveConfigToStorage()
    - If userId provided, POST to /api/config to sync to server
    - Use void operator for async calls (fire-and-forget pattern)
    - Catch and log errors, don't block UI

    Return object:
    - config: TrainerConfig | null
    - updateConfig: (updates: Partial<TrainerConfig>) => void (merge with existing)
    - isLoading: boolean

    Use useCallback for updateConfig to prevent unnecessary re-renders.

    Include JSDoc explaining hook combines client persistence (fast) with server sync (cross-device).
  </action>
  <verify>
    Hook loads config on mount, updateConfig merges changes and triggers save, debounce prevents rapid localStorage writes
  </verify>
  <done>
    useTrainerConfig hook provides config state with auto-save to localStorage and optional server sync for authenticated users
  </done>
</task>

<task type="auto">
  <name>Add database field and API endpoint for user preferences</name>
  <files>
    prisma/schema.prisma
    src/server/routes/config.routes.ts
    src/server/controllers/config.controller.ts
  </files>
  <action>
    Update Prisma User model:
    - Add optional trainerPreferences field: Json? (nullable, stores TrainerConfig as JSON)
    - Run npx prisma migrate dev --name add_trainer_preferences to create migration

    Create Express routes at /api/config:
    - GET /api/config - requireAuth middleware, return user.trainerPreferences or default config
    - POST /api/config - requireAuth middleware, validate body with trainerConfigSchema, update user.trainerPreferences, return updated config

    Create controller functions:
    - getTrainerConfig(req, res): fetch req.user.userId from prisma, return trainerPreferences JSON
    - updateTrainerConfig(req, res): validate req.body, update prisma user with trainerPreferences, return 200 with config

    Use Prisma.JsonNull for setting null values if needed (from prior auth patterns).

    Follow existing auth patterns from src/server/auth/ for requireAuth middleware usage.

    Error handling: 404 if user not found, 400 if validation fails, 500 for database errors.
  </action>
  <verify>
    npx prisma migrate dev succeeds, GET /api/config returns config for authenticated user, POST /api/config saves and persists config to database
  </verify>
  <done>
    User.trainerPreferences field exists, /api/config endpoints allow GET/POST of trainer config with authentication, database persists user preferences
  </done>
</task>

</tasks>

<verification>
- npm run typecheck passes
- npm test passes for config validation and localStorage tests
- Prisma migration applied successfully
- GET /api/config returns config (requires auth token)
- POST /api/config persists config to database
- localStorage round-trips config data correctly
</verification>

<success_criteria>
- TrainerConfig type and validation schema exist with minimum selection rules
- localStorage persistence handles schema changes gracefully (falls back to defaults)
- useTrainerConfig hook provides config state with debounced auto-save
- Database stores authenticated user preferences in User.trainerPreferences JSON field
- API endpoints allow authenticated users to fetch and update config server-side
</success_criteria>

<output>
After completion, create `.planning/phases/06-trainer-configuration/06-01-SUMMARY.md`
</output>
