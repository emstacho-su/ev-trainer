---
phase: 06-trainer-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/config/PositionFilters.tsx
  - src/components/config/PotTypeFilters.tsx
  - src/components/config/ModeToggle.tsx
  - src/components/config/GameSetup.tsx
  - src/components/config/ConfigCard.tsx
autonomous: true

must_haves:
  truths:
    - "User can select multiple positions with toggle chips"
    - "User can apply quick presets (All Positions, Blinds Only, Late Position Only)"
    - "User cannot deselect the last remaining position (min 1 enforced)"
    - "User can select multiple pot types with toggle chips"
    - "User can toggle between Preflop/Flop mode"
  artifacts:
    - path: "src/components/config/PositionFilters.tsx"
      provides: "Multi-select position filter with presets"
      min_lines: 40
    - path: "src/components/config/PotTypeFilters.tsx"
      provides: "Multi-select pot type filter"
      min_lines: 30
    - path: "src/components/config/ModeToggle.tsx"
      provides: "Preflop/Flop mode toggle"
      min_lines: 20
    - path: "src/components/config/GameSetup.tsx"
      provides: "Game type, table size, stack depth selectors"
      min_lines: 40
    - path: "src/components/config/ConfigCard.tsx"
      provides: "Reusable card wrapper for config sections"
      min_lines: 15
  key_links:
    - from: "src/components/config/PositionFilters.tsx"
      to: "props.onChange"
      via: "callback with updated position array"
      pattern: "onChange\\(Array\\.from"
    - from: "src/components/config/PotTypeFilters.tsx"
      to: "props.onChange"
      via: "callback with updated pot type array"
      pattern: "onChange\\(Array\\.from"
---

<objective>
Filter and configuration UI components: multi-select toggle chips with presets, mode toggle, game setup dropdowns, and reusable card wrapper.

Purpose: Provide reusable, validated filter components used by both lobby screen and sidebar drawer.

Output: Interactive filter components with visual feedback, preset buttons, and minimum selection enforcement.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\estac\projects\ev-trainer\.planning\PROJECT.md
@C:\Users\estac\projects\ev-trainer\.planning\ROADMAP.md
@C:\Users\estac\projects\ev-trainer\.planning\STATE.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-CONTEXT.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-RESEARCH.md
@C:\Users\estac\projects\ev-trainer\src\components\poker\atoms\Chip.tsx
</context>

<tasks>

<task type="auto">
  <name>Create reusable ConfigCard wrapper component</name>
  <files>
    src/components/config/ConfigCard.tsx
  </files>
  <action>
    Create ConfigCard component accepting props:
    - title: string (card heading)
    - subtitle?: string (optional description)
    - children: React.ReactNode (card content)

    Render structure:
    - Outer div with border, rounded corners, padding, background using theme tokens
    - Header with title (font-semibold) and optional subtitle (text-sm, muted color)
    - Content section with children

    Use Tailwind classes following project conventions:
    - Border: border-stone-300 (light) or border-stone-700 (dark)
    - Background: bg-white (light) or bg-stone-900 (dark) using CSS custom properties
    - Spacing: p-4 for padding, space-y-2 for internal spacing

    Export as default. Include JSDoc describing purpose (reusable card for lobby/sidebar config sections).
  </action>
  <verify>
    Component renders with title, subtitle, and children correctly; Tailwind classes apply consistent styling
  </verify>
  <done>
    ConfigCard component exists as reusable wrapper with title, optional subtitle, and consistent card styling
  </done>
</task>

<task type="auto">
  <name>Create PositionFilters with toggle chips and presets</name>
  <files>
    src/components/config/PositionFilters.tsx
  </files>
  <action>
    Create PositionFilters component accepting props:
    - selected: string[] (currently selected positions)
    - onChange: (positions: string[]) => void (callback with updated selection)

    Constants:
    - POSITIONS array: ['UTG', 'HJ', 'CO', 'BTN', 'SB', 'BB']
    - PRESETS object:
      - 'All Positions': all 6 positions
      - 'Blinds Only': ['SB', 'BB']
      - 'Late Position Only': ['CO', 'BTN', 'SB', 'BB']

    State:
    - Use Set for selected positions (efficient lookups and toggles)
    - Initialize from props.selected

    Toggle logic:
    - handleToggle(position): add if not selected, remove if selected
    - Enforce minimum: if selectedSet.has(position) && selectedSet.size === 1, return early (prevent deselecting last)
    - Call onChange(Array.from(selectedSet))

    Preset logic:
    - applyPreset(presetName): replace selectedSet with preset positions, call onChange

    Render:
    - Fieldset with legend "Positions"
    - Preset buttons row (flex gap-2, rounded-full border buttons)
    - Toggle chips row (flex flex-wrap gap-2)
    - Each chip button: onClick calls handleToggle, conditional classes for selected state
    - Selected: bg-stone-900 text-white (dark poker aesthetic)
    - Unselected: border border-stone-300 bg-white text-stone-900 hover:bg-stone-50

    Use 'use client' directive (interactive component). Export as default.

    Include comment explaining minimum selection enforcement prevents empty filter state.
  </action>
  <verify>
    Component renders 6 position chips and 3 preset buttons; clicking chips toggles selection; clicking preset applies preset; cannot deselect last position
  </verify>
  <done>
    PositionFilters component with toggle chips, presets, and minimum selection enforcement exists and calls onChange with updated array
  </done>
</task>

<task type="auto">
  <name>Create PotTypeFilters with toggle chips</name>
  <files>
    src/components/config/PotTypeFilters.tsx
  </files>
  <action>
    Create PotTypeFilters component accepting props:
    - selected: string[] (currently selected pot types)
    - onChange: (potTypes: string[]) => void (callback with updated selection)

    Constants:
    - POT_TYPES array: ['SRP', '3BP', '4BP'] (Single Raised Pot, 3-Bet Pot, 4-Bet Pot)

    State and logic:
    - Same Set-based tracking as PositionFilters
    - Same minimum enforcement (at least 1 pot type must be selected)
    - handleToggle(potType): same pattern as PositionFilters

    Render:
    - Fieldset with legend "Pot Types"
    - Toggle chips row (flex flex-wrap gap-2)
    - Same selected/unselected styling as PositionFilters for consistency

    No presets needed for pot types (only 3 options, less common to have presets).

    Use 'use client' directive. Export as default.

    Include JSDoc explaining SRP/3BP/4BP abbreviations for clarity.
  </action>
  <verify>
    Component renders 3 pot type chips; clicking toggles selection; cannot deselect last pot type
  </verify>
  <done>
    PotTypeFilters component with toggle chips and minimum selection enforcement exists and calls onChange with updated array
  </done>
</task>

<task type="auto">
  <name>Create ModeToggle for Preflop/Flop selection</name>
  <files>
    src/components/config/ModeToggle.tsx
  </files>
  <action>
    Create ModeToggle component accepting props:
    - value: 'PREFLOP' | 'FLOP' (current mode)
    - onChange: (mode: 'PREFLOP' | 'FLOP') => void (callback with new mode)

    Render:
    - Two-button toggle group (flex gap-2)
    - Button for "Preflop": onClick calls onChange('PREFLOP')
    - Button for "Flop": onClick calls onChange('FLOP')
    - Selected button: bg-stone-900 text-white
    - Unselected button: border border-stone-300 bg-white text-stone-900 hover:bg-stone-50
    - Both buttons rounded, px-4 py-2, font-medium

    Use 'use client' directive. Export as default.

    Include JSDoc explaining this is lobby-only (not shown in sidebar during active session).
  </action>
  <verify>
    Component renders two toggle buttons; clicking calls onChange with correct mode; selected button visually distinct
  </verify>
  <done>
    ModeToggle component toggles between Preflop and Flop modes and calls onChange with selection
  </done>
</task>

<task type="auto">
  <name>Create GameSetup with dropdowns for game type, table size, stack depth</name>
  <files>
    src/components/config/GameSetup.tsx
  </files>
  <action>
    Create GameSetup component accepting props:
    - gameType: 'CASH' | 'HU' (current game type)
    - tableSize: '6max' | '9max' (current table size)
    - stackDepth: '50bb' | '100bb' | '200bb' (current stack depth)
    - villainAlwaysRaise: boolean (villain always raise toggle)
    - onChange: (updates: Partial<{ gameType, tableSize, stackDepth, villainAlwaysRaise }>) => void

    Render:
    - Three select dropdowns + one toggle in vertical stack (space-y-3)
    - Each wrapped in label with descriptive text

    Game Type dropdown:
    - Options: Cash, Heads-Up (values: CASH, HU)
    - onChange calls props.onChange({ gameType: e.target.value })

    Table Size dropdown:
    - Options: 6-max, 9-max (values: 6max, 9max)
    - onChange calls props.onChange({ tableSize: e.target.value })

    Stack Depth dropdown:
    - Options: 50bb, 100bb, 200bb (values: 50bb, 100bb, 200bb)
    - onChange calls props.onChange({ stackDepth: e.target.value })

    Villain Always Raise toggle:
    - Checkbox or toggle switch with label "Villain Always Raises"
    - Description text: "Forces villain to always raise for aggressive practice scenarios"
    - onChange calls props.onChange({ villainAlwaysRaise: e.target.checked })
    - Styling: flex items-center gap-2 with checkbox input

    Styling:
    - Select elements: border border-stone-300 rounded px-3 py-2 w-full
    - Labels: text-sm font-medium mb-1 block

    Use 'use client' directive. Export as default.

    Include JSDoc explaining these are lobby-only settings (locked during active session).
  </action>
  <verify>
    Component renders three dropdowns with correct options; selecting option calls onChange with updated field
  </verify>
  <done>
    GameSetup component provides dropdowns for game type, table size, stack depth, and Villain Always Raise toggle, calling onChange with partial updates
  </done>
</task>

</tasks>

<verification>
- All components render without errors
- npm run typecheck passes
- PositionFilters enforces minimum 1 position selected
- PotTypeFilters enforces minimum 1 pot type selected
- Presets apply correct position arrays
- ModeToggle switches between PREFLOP and FLOP
- GameSetup dropdowns and Villain Always Raise toggle update individual fields via onChange
</verification>

<success_criteria>
- ConfigCard reusable wrapper exists with consistent styling
- PositionFilters supports multi-select with 3 presets and minimum enforcement
- PotTypeFilters supports multi-select with minimum enforcement
- ModeToggle provides Preflop/Flop mode selection
- GameSetup provides game type, table size, stack depth dropdowns and Villain Always Raise toggle (CONF-05)
- All components use 'use client' directive and follow project Tailwind conventions
</success_criteria>

<output>
After completion, create `.planning/phases/06-trainer-configuration/06-02-SUMMARY.md`
</output>
