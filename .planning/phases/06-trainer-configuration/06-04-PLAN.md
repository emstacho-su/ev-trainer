---
phase: 06-trainer-configuration
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/components/config/SessionSidebar.tsx
  - src/components/config/HamburgerButton.tsx
  - src/lib/ui/toastContext.tsx
  - src/lib/ui/ToastContainer.tsx
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can open sidebar from hamburger menu during active session"
    - "Sidebar overlays table with backdrop dim"
    - "Sidebar shows changeable filters (positions, pot types, hand count target)"
    - "Sidebar hides/disables lobby-only settings (mode, game type, table size, stack depth)"
    - "Closing sidebar or clicking backdrop dismisses sidebar"
    - "Filter changes in sidebar take effect on next hand"
  artifacts:
    - path: "src/components/config/SessionSidebar.tsx"
      provides: "Overlay drawer for mid-session configuration"
      min_lines: 60
    - path: "src/components/config/HamburgerButton.tsx"
      provides: "Hamburger menu button to toggle sidebar"
      min_lines: 20
    - path: "src/lib/ui/toastContext.tsx"
      provides: "Toast notification context and provider"
      min_lines: 50
    - path: "src/lib/ui/ToastContainer.tsx"
      provides: "Toast notification renderer"
      min_lines: 30
  key_links:
    - from: "src/components/config/SessionSidebar.tsx"
      to: "props.onConfigChange"
      via: "callback updates session config"
      pattern: "onConfigChange\\("
    - from: "src/components/config/HamburgerButton.tsx"
      to: "props.onClick"
      via: "toggles sidebar visibility"
      pattern: "onClick"
    - from: "src/lib/ui/ToastContainer.tsx"
      to: "src/lib/ui/toastContext.tsx"
      via: "useToast hook"
      pattern: "useToast\\(\\)"
---

<objective>
Session sidebar drawer with mid-session changeable filters, toast notification system, and hamburger menu toggle.

Purpose: Allow users to adjust position/pot filters and hand count target during active session without restarting.

Output: Left-side overlay drawer accessible via hamburger menu, toast notifications for filter change confirmations.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\estac\projects\ev-trainer\.planning\PROJECT.md
@C:\Users\estac\projects\ev-trainer\.planning\ROADMAP.md
@C:\Users\estac\projects\ev-trainer\.planning\STATE.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-CONTEXT.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-RESEARCH.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-01-PLAN.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create toast notification context and provider</name>
  <files>
    src/lib/ui/toastContext.tsx
    src/lib/ui/ToastContainer.tsx
  </files>
  <action>
    Create toastContext.tsx with:

    Toast interface:
    - id: string (unique identifier)
    - message: string (toast content)
    - type: 'success' | 'error' | 'info' | 'warning'
    - duration?: number (auto-dismiss in ms, undefined = permanent)

    ToastContext value:
    - toasts: Toast[] (active toasts)
    - showToast: (message: string, type?: Toast['type'], duration?: number) => void
    - removeToast: (id: string) => void

    ToastProvider component:
    - useState for toasts array
    - showToast generates random id, adds toast to array
    - If duration provided, setTimeout to removeToast after duration
    - removeToast filters toast out of array
    - Provide context value via ToastContext.Provider

    useToast custom hook:
    - useContext(ToastContext)
    - Throw error if used outside ToastProvider
    - Return context value

    Create ToastContainer.tsx:
    - useToast hook to access toasts
    - Render fixed position container (top-right corner, z-50)
    - Map over toasts array
    - Each toast: div with conditional bg color based on type, message text, close button
    - Close button onClick calls removeToast(toast.id)
    - Transition classes for slide-in/out (transition-all duration-300)

    Styling:
    - Success: bg-green-600 text-white
    - Error: bg-red-600 text-white
    - Info: bg-stone-600 text-white
    - Warning: bg-yellow-600 text-white
    - Fixed top-4 right-4, space-y-2 for stacking toasts

    Use 'use client' directive. Export ToastProvider and useToast from toastContext, default export ToastContainer.
  </action>
  <verify>
    ToastProvider wraps app, useToast hook accessible in components, showToast displays toast in top-right, removeToast dismisses toast
  </verify>
  <done>
    Toast notification system with context, provider, and container component exists and renders toasts in top-right corner
  </done>
</task>

<task type="auto">
  <name>Create HamburgerButton component</name>
  <files>
    src/components/config/HamburgerButton.tsx
  </files>
  <action>
    Create HamburgerButton component accepting props:
    - onClick: () => void (callback to toggle sidebar)

    Render:
    - Button with three horizontal lines (hamburger icon)
    - Use Unicode or SVG for icon (three stacked divs with bg-current works)
    - Button styling: fixed top-4 left-4, z-40, bg-stone-900 text-white rounded p-2
    - Hover effect: hover:bg-stone-700
    - Aria-label: "Open settings"

    Use 'use client' directive. Export as default.

    Include JSDoc explaining this is session page toggle for sidebar (not shown on lobby).
  </action>
  <verify>
    Button renders in top-left, clicking calls onClick callback, icon visible and styled
  </verify>
  <done>
    HamburgerButton component renders fixed top-left and calls onClick to toggle sidebar
  </done>
</task>

<task type="auto">
  <name>Create SessionSidebar drawer overlay component</name>
  <files>
    src/components/config/SessionSidebar.tsx
  </files>
  <action>
    Create SessionSidebar component accepting props:
    - isOpen: boolean (sidebar visibility state)
    - config: TrainerConfig (current session config)
    - onConfigChange: (updates: Partial<TrainerConfig>) => void (callback to update config)
    - onClose: () => void (callback to close sidebar)

    Hooks:
    - useRef for sidebar div (for click-outside detection)
    - useEffect with mousedown listener to detect clicks outside sidebar, call onClose
    - useToast for showing filter change notifications

    Layout:
    - Backdrop div: fixed inset-0 z-40 bg-black/30, visible when isOpen, onClick calls onClose
    - Sidebar div: fixed left-0 top-0 z-50 h-screen w-80 bg-white shadow-lg
    - Transform transition: translate-x-0 when isOpen, -translate-x-full when closed
    - Transition duration: duration-300 for smooth slide
    - Sidebar content: flex flex-col gap-4 overflow-y-auto p-4

    Content sections:
    - Header: h2 "Session Settings", close button (× icon) onClick calls onClose
    - Locked settings fieldset (disabled, opacity-50):
      - Legend: "Locked (lobby-only)"
      - Text explaining Mode, Game Type, Table Size, Stack Depth cannot be changed mid-session
    - Changeable settings:
      - PositionFilters (reuse from Plan 06-02)
      - PotTypeFilters (reuse from Plan 06-02)
      - Hand Count Target: input type="number", value from config.handCountTarget, onChange calls onConfigChange({ handCountTarget: parseInt(value) })
    - When filter changes, call onConfigChange and showToast("Filters will apply on next hand", "info")

    Use 'use client' directive. Export as default.

    Include JSDoc explaining sidebar mirrors lobby but hides lobby-only settings, changes take effect next hand.
  </action>
  <verify>
    Sidebar slides in from left when isOpen=true, backdrop dims background, clicking outside closes sidebar, filter changes call onConfigChange and show toast
  </verify>
  <done>
    SessionSidebar component renders as overlay drawer with changeable filters, locked settings displayed as disabled, close on backdrop click
  </done>
</task>

<task type="auto">
  <name>Integrate ToastProvider into app layout</name>
  <files>
    src/app/layout.tsx
  </files>
  <action>
    Update root layout to wrap children with ToastProvider.

    Import ToastProvider from src/lib/ui/toastContext.tsx.
    Import ToastContainer from src/lib/ui/ToastContainer.tsx.

    Wrap existing {children} with <ToastProvider>.

    Add <ToastContainer /> as sibling to {children} (renders toasts globally).

    Maintain existing layout structure (ThemeProvider, fonts, etc.).

    Example structure:
    ```tsx
    <ToastProvider>
      {children}
      <ToastContainer />
    </ToastProvider>
    ```

    Ensure ToastProvider wraps all pages so useToast hook works throughout app.
  </action>
  <verify>
    App loads without errors, toasts can be shown from any page, ToastContainer renders in top-right
  </verify>
  <done>
    ToastProvider and ToastContainer integrated into root layout, toast system available globally
  </done>
</task>

</tasks>

<verification>
- HamburgerButton renders and calls onClick
- SessionSidebar slides in/out with smooth transition
- Backdrop dims background when sidebar open
- Clicking backdrop or close button closes sidebar
- Locked settings shown as disabled in sidebar
- PositionFilters and PotTypeFilters functional in sidebar
- Hand count target input updates config
- Filter changes show toast notification
- ToastContainer renders toasts in top-right corner
</verification>

<success_criteria>
- Toast notification system with ToastProvider, useToast hook, and ToastContainer exists
- HamburgerButton component toggles sidebar visibility
- SessionSidebar renders as left-side overlay drawer with backdrop
- Sidebar shows changeable filters (positions, pot types, hand count target)
- Sidebar hides/disables lobby-only settings with explanation
- Click outside or close button dismisses sidebar
- Filter changes call onConfigChange and show toast notification
</success_criteria>

<output>
After completion, create `.planning/phases/06-trainer-configuration/06-04-SUMMARY.md`
</output>
