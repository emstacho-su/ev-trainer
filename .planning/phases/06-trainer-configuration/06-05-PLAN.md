---
phase: 06-trainer-configuration
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/components/config/DrillSuggestions.tsx
  - src/components/config/SessionSummary.tsx
  - src/lib/v2/api/drillSuggestions.ts
  - src/server/routes/drill.routes.ts
  - src/server/controllers/drill.controller.ts
autonomous: true

must_haves:
  truths:
    - "User sees top 3 weakest spot suggestions on lobby (if enough data exists)"
    - "User sees placeholder when insufficient data for drill suggestions"
    - "User can click drill suggestion to pre-fill lobby filters"
    - "User sees session summary screen when session ends"
    - "User can click Play Again to restart with same config"
    - "User can click Back to Lobby to change settings"
  artifacts:
    - path: "src/components/config/DrillSuggestions.tsx"
      provides: "Drill suggestion cards on lobby"
      min_lines: 40
    - path: "src/components/config/SessionSummary.tsx"
      provides: "Post-session summary screen"
      min_lines: 50
    - path: "src/lib/v2/api/drillSuggestions.ts"
      provides: "Logic to compute top 3 weakest spots from stats"
      exports: ["computeDrillSuggestions"]
    - path: "src/server/routes/drill.routes.ts"
      provides: "GET /api/drills/suggestions endpoint"
      min_lines: 10
  key_links:
    - from: "src/components/config/DrillSuggestions.tsx"
      to: "/api/drills/suggestions"
      via: "fetch suggestions on mount"
      pattern: "fetch.*api/drills/suggestions"
    - from: "src/components/config/DrillSuggestions.tsx"
      to: "props.onSelectDrill"
      via: "callback with pre-filled config"
      pattern: "onSelectDrill\\("
    - from: "src/components/config/SessionSummary.tsx"
      to: "props.onPlayAgain"
      via: "callback to restart session"
      pattern: "onPlayAgain\\(\\)"
---

<objective>
Drill suggestions on lobby and session summary screen for post-session actions.

Purpose: Surface weakest spots as drill opportunities and provide clear post-session flow (Play Again or Back to Lobby).

Output: Drill suggestion cards on lobby (top 3 weak spots), session summary screen with replay options.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\estac\projects\ev-trainer\.planning\PROJECT.md
@C:\Users\estac\projects\ev-trainer\.planning\ROADMAP.md
@C:\Users\estac\projects\ev-trainer\.planning\STATE.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-CONTEXT.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-RESEARCH.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-01-PLAN.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-02-PLAN.md
@C:\Users\estac\projects\ev-trainer\.planning\phases\06-trainer-configuration\06-03-PLAN.md
@C:\Users\estac\projects\ev-trainer\prisma\schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create drill suggestion computation logic</name>
  <files>
    src/lib/v2/api/drillSuggestions.ts
  </files>
  <action>
    Create computeDrillSuggestions function:

    Input: userId: string
    Output: Promise<DrillSuggestion[]> (up to 3 suggestions)

    DrillSuggestion interface:
    - spotLabel: string (e.g., "BB vs CO")
    - accuracy: number (percentage)
    - avgEvLoss: number (EV loss per hand)
    - positions: string[] (pre-filled position filter)
    - potTypes: string[] (pre-filled pot type filter)

    Logic:
    - Query prisma.spotStat where userId matches
    - Filter where totalDecisions >= 10 (minimum threshold for reliable suggestions)
    - Compute accuracy: correctDecisions / totalDecisions
    - Sort by avgEvLoss descending (worst first) or accuracy ascending (lowest first)
    - Take top 3
    - Map to DrillSuggestion objects

    If fewer than 3 spots with sufficient data, return what's available (empty array if none).

    Use Prisma client singleton pattern from existing codebase (src/lib/prisma/client.ts).

    Export computeDrillSuggestions and DrillSuggestion type.

    Include JSDoc explaining threshold (10 hands minimum) and sorting logic (worst spots first).
  </action>
  <verify>
    Function queries SpotStat table, filters by totalDecisions >= 10, returns top 3 worst spots with pre-filled filter arrays
  </verify>
  <done>
    computeDrillSuggestions function computes top 3 weakest spots from user's SpotStat data with minimum threshold
  </done>
</task>

<task type="auto">
  <name>Create drill suggestions API endpoint</name>
  <files>
    src/server/routes/drill.routes.ts
    src/server/controllers/drill.controller.ts
  </files>
  <action>
    Create Express router at /api/drills.

    Route: GET /api/drills/suggestions
    - Middleware: requireAuth (user must be logged in)
    - Handler: drillController.getSuggestions

    Controller function getSuggestions:
    - Extract userId from req.user.userId (set by requireAuth middleware)
    - Call computeDrillSuggestions(userId)
    - Return 200 with suggestions array
    - Error handling: 401 if not authenticated, 500 if database error

    Mount drill routes in src/server/app.ts:
    - Import drillRoutes from drill.routes.ts
    - app.use('/api/drills', drillRoutes)

    Follow existing route patterns from src/server/routes/session.routes.ts and auth patterns.

    Export router as default from drill.routes.ts.
  </action>
  <verify>
    GET /api/drills/suggestions returns suggestions array for authenticated user, 401 for unauthenticated
  </verify>
  <done>
    /api/drills/suggestions endpoint returns drill suggestions for authenticated users
  </done>
</task>

<task type="auto">
  <name>Create DrillSuggestions component for lobby</name>
  <files>
    src/components/config/DrillSuggestions.tsx
  </files>
  <action>
    Create DrillSuggestions component accepting props:
    - onSelectDrill: (drill: Partial<TrainerConfig>) => void (callback to pre-fill lobby config)

    Hooks:
    - useState for suggestions (DrillSuggestion[] | null)
    - useState for loading (boolean)
    - useEffect to fetch from /api/drills/suggestions on mount (if authenticated)

    Fetch logic:
    - On mount, GET /api/drills/suggestions
    - If 401 (not authenticated), show guest message: "Sign in to see drill suggestions"
    - If 200, set suggestions array
    - If empty array, show placeholder: "Play more hands to unlock drill suggestions"
    - On error, log and show placeholder

    Render:
    - Loading state: Spinner or "Loading suggestions..."
    - Guest state: Message with link to login
    - No data state: Placeholder message
    - Suggestions state: Map over suggestions array, render card for each

    Suggestion card:
    - Spot label (e.g., "BB vs CO")
    - Accuracy percentage (e.g., "42% accuracy")
    - Avg EV loss (e.g., "-2.3 EV/hand")
    - Click card calls onSelectDrill({ positions: drill.positions, potTypes: drill.potTypes })
    - Styling: border rounded p-3 cursor-pointer hover:bg-stone-50

    Use 'use client' directive. Export as default.

    Include JSDoc explaining suggestions are top 3 weakest spots, clicking pre-fills lobby filters.
  </action>
  <verify>
    Component fetches suggestions on mount, shows placeholder when no data, clicking suggestion calls onSelectDrill with pre-filled config
  </verify>
  <done>
    DrillSuggestions component fetches and displays top 3 weakest spots, allows clicking to pre-fill lobby filters
  </done>
</task>

<task type="auto">
  <name>Create SessionSummary screen component</name>
  <files>
    src/components/config/SessionSummary.tsx
  </files>
  <action>
    Create SessionSummary component accepting props:
    - handsPlayed: number (total hands in session)
    - accuracy: number (percentage of correct decisions)
    - avgEvLoss: number (average EV loss per hand)
    - onPlayAgain: () => void (callback to restart with same config)
    - onBackToLobby: () => void (callback to return to lobby)

    Render:
    - Container: max-w-2xl mx-auto, centered vertically
    - Header: h1 "Session Complete"
    - Stats section (grid layout):
      - Hands Played: {handsPlayed}
      - Accuracy: {accuracy.toFixed(1)}%
      - Avg EV Loss: {avgEvLoss.toFixed(2)} BB/hand
    - Button section (flex gap-4):
      - "Play Again" button: bg-stone-900 text-white, onClick calls onPlayAgain
      - "Back to Lobby" button: border border-stone-300, onClick calls onBackToLobby

    Styling:
    - Stats cards: border rounded p-4, each stat in separate card
    - Buttons: px-6 py-3 rounded font-semibold
    - Spacing: space-y-6 for vertical sections

    Use 'use client' directive. Export as default.

    Include JSDoc explaining this is shown when session ends (user clicks Stop or hand target reached).
  </action>
  <verify>
    Component renders session stats, clicking Play Again calls onPlayAgain, clicking Back to Lobby calls onBackToLobby
  </verify>
  <done>
    SessionSummary component displays session stats and provides Play Again and Back to Lobby buttons
  </done>
</task>

<task type="auto">
  <name>Integrate DrillSuggestions into TrainerLobby</name>
  <files>
    src/components/config/TrainerLobby.tsx
  </files>
  <action>
    Update TrainerLobby component to include DrillSuggestions.

    Import DrillSuggestions from src/components/config/DrillSuggestions.tsx.

    Add DrillSuggestions in Essentials/Advanced card layout:
    - Render DrillSuggestions in second card of first grid row (beside Essentials card)
    - Pass onSelectDrill callback that updates config state with pre-filled positions and pot types

    onSelectDrill logic:
    - Receives drill: { positions: string[], potTypes: string[] }
    - Call updateConfig({ positions: drill.positions, potTypes: drill.potTypes })
    - No navigation needed (user still on lobby, can review and click Start Training)

    Maintain existing layout (Essentials card, Advanced Filters, Start button).

    Update grid to lg:grid-cols-2 for side-by-side cards.
  </action>
  <verify>
    Lobby shows DrillSuggestions card beside Essentials, clicking suggestion pre-fills filters on lobby
  </verify>
  <done>
    TrainerLobby integrates DrillSuggestions, clicking suggestion pre-fills lobby filters
  </done>
</task>

</tasks>

<verification>
- computeDrillSuggestions queries SpotStat with minimum threshold
- GET /api/drills/suggestions returns suggestions for authenticated users
- DrillSuggestions component fetches and displays top 3 weakest spots
- Clicking drill suggestion pre-fills lobby filters
- SessionSummary component renders stats and action buttons
- TrainerLobby includes DrillSuggestions card
</verification>

<success_criteria>
- computeDrillSuggestions function computes top 3 weakest spots from SpotStat
- /api/drills/suggestions endpoint returns drill suggestions
- DrillSuggestions component shows top 3 weak spots or placeholder
- Clicking drill pre-fills lobby position and pot type filters
- SessionSummary component displays session stats with Play Again and Back to Lobby buttons
- TrainerLobby integrates DrillSuggestions in card layout
</success_criteria>

<output>
After completion, create `.planning/phases/06-trainer-configuration/06-05-SUMMARY.md`
</output>
