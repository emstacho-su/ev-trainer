---
phase: 07-range-visualization
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/range/ActionLegend.tsx
  - src/components/range/EquityBreakdown.tsx
  - src/components/range/EquityTable.tsx
  - src/lib/range/context.ts
  - src/components/range/index.ts
autonomous: true

must_haves:
  truths:
    - "Action legend displays frequency summary with clickable filter buttons"
    - "Equity breakdown shows hand categories with percentages"
    - "Tabbed view toggles between Hand Strength and Action Groups"
  artifacts:
    - path: "src/components/range/ActionLegend.tsx"
      provides: "Clickable action filter with frequency display"
      exports: ["ActionLegend"]
      min_lines: 40
    - path: "src/components/range/EquityBreakdown.tsx"
      provides: "Tabbed equity view for hero and villain"
      exports: ["EquityBreakdown"]
      min_lines: 50
    - path: "src/components/range/EquityTable.tsx"
      provides: "Hand category breakdown table"
      exports: ["EquityTable"]
      min_lines: 30
    - path: "src/lib/range/context.ts"
      provides: "React context for filter state sharing"
      exports: ["RangeContext", "RangeContextProvider"]
  key_links:
    - from: "src/components/range/ActionLegend.tsx"
      to: "src/lib/range/context.ts"
      via: "useContext hook"
      pattern: "useContext\\(RangeContext\\)"
    - from: "src/components/range/EquityBreakdown.tsx"
      to: "src/lib/range/rangeHelpers.ts"
      via: "categorizeHandStrength import"
      pattern: "categorizeHandStrength"
---

<objective>
Build interactive legend and equity breakdown components for range analysis.

Purpose: Create the filtering and analysis UI elements. ActionLegend provides clickable action filters (Fold/Call/Raise) with frequency summaries below each grid. EquityBreakdown displays tabbed views of hand categories (Hand Strength vs Action Groups) for both hero and villain with cross-reference filtering.

Output: Three components (ActionLegend, EquityBreakdown, EquityTable) plus React context for shared filter state.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-range-visualization/07-CONTEXT.md
@.planning/phases/07-range-visualization/07-RESEARCH.md

# Prior plan established types and helpers
@.planning/phases/07-range-visualization/07-01-SUMMARY.md

# Phase 4 established button interaction patterns
@.planning/phases/04-table-ui-foundation/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create React context for filter state sharing</name>
  <files>src/lib/range/context.ts</files>
  <action>
Create `src/lib/range/context.ts` for shared filtering state (following RESEARCH.md Pattern 1).

Context structure:
```typescript
import { createContext } from 'react';
import type { ActionType, EquityCategory } from './types';

interface RangeContextValue {
  selectedAction: ActionType | null;
  setSelectedAction: (action: ActionType | null) => void;
  selectedEquityCategory: EquityCategory | null;
  setSelectedEquityCategory: (category: EquityCategory | null) => void;
}

export const RangeContext = createContext<RangeContextValue>({
  selectedAction: null,
  setSelectedAction: () => {},
  selectedEquityCategory: null,
  setSelectedEquityCategory: () => {},
});
```

Export both context and a provider component pattern (though provider will be used in 07-04 modal, define interface here).

Follow Phase 4 pattern: Context established in lib, consumed via useContext in components.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`

Check exports:
```typescript
import { RangeContext } from '@/lib/range/context';
```
  </verify>
  <done>
- RangeContext created with selectedAction and selectedEquityCategory state
- Context exported from @/lib/range/context
- TypeScript compilation succeeds
  </done>
</task>

<task type="auto">
  <name>Create ActionLegend with clickable filter buttons</name>
  <files>src/components/range/ActionLegend.tsx</files>
  <action>
Create `src/components/range/ActionLegend.tsx` following RESEARCH.md Pattern 3: Accessible Filtered Grid.

Component structure:
- Accept props: `range` (RangeData), `title` (string like "Hero Range - Action Frequency")
- Use `useContext(RangeContext)` to access selectedAction and setSelectedAction
- Calculate action frequency totals from range.hands:
  - Iterate all hands, sum frequencies per action type
  - Display as percentages: `Fold: 78.1%, Call: 21.9%, Raise: 0.0%`
- Render clickable buttons for each action with frequency display
- Button states:
  - Selected: `ring-2 ring-offset-1 ring-blue-400` (matches selected state)
  - Unselected: `opacity-60 hover:opacity-100` for subtle inactive state
  - Click toggles selection (click same action deselects, returns to null)
- Buttons use action colors as background (visual consistency with cells):
  - `bg-[hsl(var(--color-action-fold))]` for Fold button
  - `bg-[hsl(var(--color-action-call))]` for Call button
  - `bg-[hsl(var(--color-action-raise))]` for Raise button
- Layout: horizontal flex row with gap-4

Follow CONTEXT.md: "Clickable action legend at bottom of each grid. Clicking an action filters the grid to highlight only that action."

Per CONTEXT.md: "Each grid (hero/villain) has its own action frequency summary below it" - ActionLegend is per-grid, not shared.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`

Visual check with mock range:
```tsx
<ActionLegend
  range={mockRangeData}
  title="Hero Range - Action Frequency"
/>
```

Verify:
- Action buttons display with correct frequencies (e.g., "Fold 78.1%")
- Clicking button sets selectedAction in context
- Selected button shows blue ring, others dim
- Clicking again deselects (selectedAction becomes null)
  </verify>
  <done>
- ActionLegend.tsx exports component
- Calculates and displays action frequency percentages
- Clickable buttons toggle filter state via RangeContext
- Selected state visually indicated with blue ring
  </done>
</task>

<task type="auto">
  <name>Create EquityBreakdown with tabbed hand category views</name>
  <files>
    src/components/range/EquityBreakdown.tsx
    src/components/range/EquityTable.tsx
    src/components/range/index.ts
  </files>
  <action>
Create `src/components/range/EquityTable.tsx` helper component first:
- Accept props: `range` (RangeData), `playerName` (string like "Hero"), `viewMode` ('hand-strength' | 'action-groups')
- Use categorizeHandStrength helper from @/lib/range/rangeHelpers
- Group hands by category based on viewMode:
  - Hand Strength: Premium pairs (AA-QQ), Medium pairs (JJ-77), Small pairs (66-22), Broadway, Suited connectors, Suited gappers, Offsuit
  - Action Groups: Pure raise, Mixed raise/call, Pure call, Pure fold
- Display as table with columns: Category | Count | Percentage
- Clicking a category row highlights those hands in grid (sets selectedEquityCategory in context)
- Selected row shows blue background highlight

Create `src/components/range/EquityBreakdown.tsx`:
- Accept props: `hero` (RangeData), `villain` (RangeData)
- Local state: `viewMode` ('hand-strength' | 'action-groups'), defaults to 'hand-strength'
- Render tab buttons to toggle viewMode:
  - "Hand Strength" | "Action Groups"
  - Selected tab: `bg-blue-600 text-white`
  - Unselected tab: `bg-gray-700 text-gray-300 hover:bg-gray-600`
- Render two EquityTable components side-by-side:
  - Left: `<EquityTable range={hero} playerName="Hero" viewMode={viewMode} />`
  - Right: `<EquityTable range={villain} playerName="Villain" viewMode={viewMode} />`
- Layout: flex row with gap-8 on desktop, flex-col on mobile (responsive)

Per CONTEXT.md: "Equity breakdown panel position (right side vs below grids — responsive considerations)" - Decision: Below grids for clearer layout, responsive flex-col on mobile.

Update `src/components/range/index.ts` barrel export to include ActionLegend, EquityBreakdown, EquityTable.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`

Visual check:
```tsx
<EquityBreakdown hero={mockHeroRange} villain={mockVillainRange} />
```

Verify:
- Tab buttons toggle between Hand Strength and Action Groups
- Both hero and villain equity tables display side-by-side
- Hand categories show correct percentages
- Clicking category row sets selectedEquityCategory in context
- Selected category highlighted with blue background
- Responsive: stacks vertically on mobile (<768px)
  </verify>
  <done>
- EquityTable.tsx categorizes hands and displays percentages
- EquityBreakdown.tsx renders tabbed view with hero/villain tables
- Tab state toggles between Hand Strength and Action Groups views
- Category click sets selectedEquityCategory for cross-reference filtering
- Barrel export updated to include new components
  </done>
</task>

</tasks>

<verification>
Integration test:
```typescript
import { ActionLegend, EquityBreakdown, EquityTable } from '@/components/range';
import { RangeContext } from '@/lib/range/context';
```

Verify context flow:
1. Render ActionLegend and EquityBreakdown with shared RangeContext
2. Click action in legend, verify selectedAction updates
3. Click equity category, verify selectedEquityCategory updates
4. Verify both filters can coexist (action filter + equity category filter)
</verification>

<success_criteria>
- ActionLegend displays action frequencies with clickable filter buttons
- EquityBreakdown shows tabbed Hand Strength / Action Groups views
- EquityTable categorizes hands correctly (premium pairs AA-QQ, etc.)
- RangeContext shares filter state across components
- Selected states visually indicated with blue highlights/rings
- Responsive layout works on mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/07-range-visualization/07-03-SUMMARY.md`
</output>
