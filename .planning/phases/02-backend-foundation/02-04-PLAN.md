---
phase: 02-backend-foundation
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - .github/workflows/deploy.yml
  - terraform/main.tf
  - terraform/variables.tf
  - terraform/outputs.tf
  - ecosystem.config.js
  - .env.production.example
  - package.json
autonomous: false

user_setup:
  - service: aws
    why: "Production infrastructure hosting"
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "AWS Console -> IAM -> Users -> Security Credentials"
      - name: AWS_SECRET_ACCESS_KEY
        source: "AWS Console -> IAM -> Users -> Security Credentials"
      - name: DATABASE_URL
        source: "Generated after RDS instance created by Terraform"
    dashboard_config:
      - task: "Create IAM user with EC2, RDS, and VPC permissions"
        location: "AWS Console -> IAM -> Users -> Add user"
      - task: "Configure GitHub secrets for CI/CD"
        location: "GitHub -> Repository -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "Express API server deployed to AWS EC2 instance"
    - "PostgreSQL database running on AWS RDS"
    - "GitHub Actions CI/CD pipeline deploys on push to main branch"
    - "Production environment uses separate DATABASE_URL from development"
    - "SSL/TLS enabled for production traffic"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions deployment workflow"
      contains: "deploy-to-aws"
      min_lines: 50
    - path: "terraform/main.tf"
      provides: "Infrastructure as code for EC2 and RDS"
      contains: "aws_instance"
      min_lines: 100
    - path: "ecosystem.config.js"
      provides: "PM2 process manager configuration"
      contains: "pm2"
    - path: ".env.production.example"
      provides: "Production environment variable template"
      contains: "DATABASE_URL"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "terraform/main.tf"
      via: "Terraform init and apply"
      pattern: "terraform apply"
    - from: "ecosystem.config.js"
      to: "src/server/index.ts"
      via: "PM2 starts Express server"
      pattern: "script.*server/index"
    - from: "terraform/main.tf"
      to: "AWS RDS instance"
      via: "Terraform resource creation"
      pattern: "aws_db_instance"
---

<objective>
Deploy Express API to AWS EC2 with RDS PostgreSQL database and GitHub Actions CI/CD pipeline

Purpose: Fulfills BACK-08 requirement for production AWS deployment with automated deployment pipeline
Output: Production-ready infrastructure on AWS with continuous deployment from GitHub
</objective>

<execution_context>
@C:\Users\estac\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\estac\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-foundation/02-RESEARCH.md
@.planning/phases/02-backend-foundation/02-01-PLAN.md
@.planning/phases/02-backend-foundation/02-02-PLAN.md
@.planning/phases/02-backend-foundation/02-03-PLAN.md

# Server entry point
@src/server/index.ts

# Prisma schema for RDS migration
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Terraform infrastructure configuration for EC2 and RDS</name>
  <files>terraform/main.tf, terraform/variables.tf, terraform/outputs.tf</files>
  <action>
Create Terraform configuration for AWS infrastructure following AWS best practices.

**terraform/main.tf:**

1. Configure AWS provider (region: us-east-1 or user preference)
2. Create VPC with public and private subnets
3. Create security groups:
   - EC2 security group: Allow inbound 80, 443, 22 (SSH), outbound all
   - RDS security group: Allow inbound 5432 from EC2 security group only
4. Create RDS PostgreSQL instance:
   - Engine: postgres 15.x
   - Instance class: db.t3.micro (free tier eligible)
   - Allocated storage: 20 GB
   - Multi-AZ: false (for cost savings in dev/staging)
   - Publicly accessible: false (private subnet)
   - Backup retention: 7 days
   - Parameter group: default.postgres15
   - Database name: ev_trainer
5. Create EC2 instance:
   - AMI: Ubuntu 22.04 LTS (use data source to get latest)
   - Instance type: t3.micro (free tier eligible)
   - User data script to install:
     - Node.js 20.x
     - PM2 process manager
     - Git
     - PostgreSQL client tools
   - Associate with public subnet
   - Assign Elastic IP
6. Create IAM role for EC2 with:
   - CloudWatch Logs write permissions
   - SSM Session Manager access (for SSH alternative)

**terraform/variables.tf:**

1. Define variables:
   - aws_region (default: "us-east-1")
   - instance_type (default: "t3.micro")
   - db_instance_class (default: "db.t3.micro")
   - db_password (sensitive: true, no default - must be provided)
   - db_username (default: "postgres")
   - ssh_key_name (for EC2 access)
   - allowed_ssh_cidr (default: "0.0.0.0/0" for initial setup, recommend tightening)

**terraform/outputs.tf:**

1. Output values:
   - ec2_public_ip
   - rds_endpoint
   - database_url (constructed from RDS endpoint, username, password, database name)

Use remote state backend (S3 + DynamoDB) for team collaboration (commented out for initial setup).

**Important:** Do NOT run terraform apply yet - this happens in CI/CD workflow. Just create the configuration files.
  </action>
  <verify>
Run `terraform init` in terraform/ directory to initialize providers.
Run `terraform validate` to check syntax.
Run `terraform plan` to preview infrastructure (requires AWS credentials).
  </verify>
  <done>
terraform/main.tf defines VPC, subnets, security groups, EC2, and RDS resources.
terraform/variables.tf defines all configurable parameters.
terraform/outputs.tf exports EC2 IP and RDS endpoint.
Terraform validates successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PM2 configuration and production environment template</name>
  <files>ecosystem.config.js, .env.production.example, package.json</files>
  <action>
Configure PM2 process manager for production Node.js deployment.

**ecosystem.config.js:**

Create PM2 ecosystem file:
```javascript
module.exports = {
  apps: [
    {
      name: 'ev-trainer-api',
      script: './dist/server/index.js',
      instances: 2, // Use cluster mode for load balancing
      exec_mode: 'cluster',
      env: {
        NODE_ENV: 'production',
        PORT: 4000,
      },
      error_file: './logs/pm2-error.log',
      out_file: './logs/pm2-out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
      merge_logs: true,
      autorestart: true,
      max_memory_restart: '500M',
      watch: false,
    },
  ],
};
```

**env.production.example:**

Create production environment template:
```
NODE_ENV=production
PORT=4000
DATABASE_URL="postgresql://postgres:PASSWORD@RDS_ENDPOINT:5432/ev_trainer?schema=public"
ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"
```

**package.json scripts:**

Add production scripts:
```json
"scripts": {
  "server:dev": "nodemon --watch src/server --exec ts-node src/server/index.ts",
  "server:build": "tsc --project tsconfig.server.json",
  "server:start": "node dist/server/index.js",
  "pm2:start": "pm2 start ecosystem.config.js",
  "pm2:stop": "pm2 stop ecosystem.config.js",
  "pm2:restart": "pm2 restart ecosystem.config.js",
  "pm2:logs": "pm2 logs ev-trainer-api"
}
```

Create logs/ directory in .gitignore.
  </action>
  <verify>
Run `npx pm2 ecosystem ecosystem.config.js` to validate PM2 config syntax.
Check that .env.production.example exists with correct placeholders.
Check that package.json has pm2:* scripts.
  </verify>
  <done>
ecosystem.config.js configures PM2 with cluster mode (2 instances).
.env.production.example provides template for production environment variables.
package.json has pm2:start, pm2:stop, pm2:restart, pm2:logs scripts.
logs/ directory in .gitignore.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Actions CI/CD deployment workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create GitHub Actions workflow for automated deployment to AWS.

**.github/workflows/deploy.yml:**

Define deployment pipeline:
```yaml
name: Deploy to AWS

on:
  push:
    branches:
      - main
  workflow_dispatch: # Manual trigger

env:
  NODE_VERSION: '20.x'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run server:build

      - name: Run tests
        run: npm test

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform init
        working-directory: ./terraform
        run: terraform init

      - name: Terraform plan
        working-directory: ./terraform
        run: terraform plan -out=tfplan
        env:
          TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
          TF_VAR_ssh_key_name: ${{ secrets.SSH_KEY_NAME }}

      - name: Terraform apply
        working-directory: ./terraform
        run: terraform apply -auto-approve tfplan

      - name: Get EC2 instance IP
        id: ec2-ip
        working-directory: ./terraform
        run: echo "ip=$(terraform output -raw ec2_public_ip)" >> $GITHUB_OUTPUT

      - name: Deploy application to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ steps.ec2-ip.outputs.ip }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/ev-trainer || git clone https://github.com/${{ github.repository }} /home/ubuntu/ev-trainer
            cd /home/ubuntu/ev-trainer
            git pull origin main
            npm ci --production
            npm run server:build
            npx prisma migrate deploy
            echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > .env
            echo "ALLOWED_ORIGINS=${{ secrets.ALLOWED_ORIGINS }}" >> .env
            npm run pm2:restart || npm run pm2:start

      - name: Verify deployment
        run: |
          sleep 10
          curl -f http://${{ steps.ec2-ip.outputs.ip }}:4000/health || exit 1
```

**Required GitHub Secrets:**
- AWS_ACCESS_KEY_ID
- AWS_SECRET_ACCESS_KEY
- DB_PASSWORD
- SSH_KEY_NAME
- SSH_PRIVATE_KEY
- DATABASE_URL (constructed from Terraform outputs)
- ALLOWED_ORIGINS

This workflow:
1. Builds and tests on every push to main
2. Provisions/updates AWS infrastructure with Terraform
3. Deploys application to EC2 via SSH
4. Runs database migrations
5. Restarts PM2 processes
6. Verifies health endpoint

**Important:** This task creates the workflow file but does NOT trigger deployment. User must configure GitHub secrets first.
  </action>
  <verify>
Check that .github/workflows/deploy.yml exists and is valid YAML.
Run `yamllint .github/workflows/deploy.yml` to validate syntax (if yamllint installed).
Verify workflow references all required secrets.
  </verify>
  <done>
.github/workflows/deploy.yml defines complete CI/CD pipeline.
Workflow triggers on push to main branch or manual dispatch.
Pipeline builds, tests, provisions infrastructure, and deploys application.
All required GitHub secrets documented in workflow comments.
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Configure AWS credentials and GitHub secrets</name>
  <action>
Set up AWS IAM user and configure GitHub repository secrets for deployment.

**AWS Setup:**
1. Create IAM user in AWS Console:
   - Go to IAM -> Users -> Add user
   - User name: "ev-trainer-deployer"
   - Attach policies: AmazonEC2FullAccess, AmazonRDSFullAccess, AmazonVPCFullAccess
   - Generate access keys (save AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY)

2. Generate SSH key pair for EC2 access:
   - Run locally: `ssh-keygen -t rsa -b 4096 -f ~/.ssh/ev-trainer-ec2`
   - Import public key to AWS EC2 -> Key Pairs
   - Save key name for TF_VAR_ssh_key_name

**GitHub Secrets Setup:**
1. Go to GitHub repository -> Settings -> Secrets and variables -> Actions
2. Add repository secrets:
   - AWS_ACCESS_KEY_ID: (from IAM user)
   - AWS_SECRET_ACCESS_KEY: (from IAM user)
   - DB_PASSWORD: (generate strong password for RDS)
   - SSH_KEY_NAME: (EC2 key pair name from AWS)
   - SSH_PRIVATE_KEY: (contents of ~/.ssh/ev-trainer-ec2 private key)
   - ALLOWED_ORIGINS: "https://yourdomain.com" (update with actual domain)

3. After first Terraform apply, get RDS endpoint and add:
   - DATABASE_URL: "postgresql://postgres:PASSWORD@RDS_ENDPOINT:5432/ev_trainer?schema=public"

**Verification:**
- All 7 secrets configured in GitHub
- AWS credentials have necessary permissions
- SSH key pair uploaded to AWS
  </action>
  <instructions>
This step requires manual configuration of AWS IAM and GitHub secrets. The deployment workflow cannot run until these secrets are configured.

Once secrets are configured, the next push to main branch will trigger automatic deployment to AWS.
  </instructions>
  <resume-signal>Type "configured" when AWS IAM user and GitHub secrets are set up</resume-signal>
</task>

</tasks>

<verification>
1. Run `terraform validate` in terraform/ directory - configuration is valid
2. Run `terraform plan` to preview infrastructure (requires AWS credentials)
3. Check that .github/workflows/deploy.yml is valid YAML
4. Verify ecosystem.config.js has correct PM2 configuration
5. After user configures secrets, push to main triggers deployment workflow
6. Monitor GitHub Actions workflow execution
7. Verify deployment: `curl http://<EC2_IP>:4000/health` returns 200
8. Check PM2 processes on EC2: `pm2 list` shows 2 instances running
</verification>

<success_criteria>
- Terraform configuration creates VPC, EC2, and RDS infrastructure
- PM2 configuration runs Express server in cluster mode
- GitHub Actions workflow automates build, test, and deployment
- Production environment uses RDS PostgreSQL (not local Docker)
- EC2 instance serves API on port 4000 with health check endpoint
- Database migrations applied automatically on deployment
- SSL/TLS ready for NGINX reverse proxy (Phase 3)
- BACK-08 requirement fulfilled (AWS deployment)
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-foundation/02-04-SUMMARY.md`
</output>
