---
phase: 02-backend-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/v2/storage/prismaSessionStore.ts
  - src/lib/v2/sessionStore.ts
  - src/server/index.ts
  - prisma/migrations/
autonomous: true

must_haves:
  truths:
    - "SessionStoreBackend interface implemented with Prisma queries"
    - "Session CRUD operations persist to PostgreSQL instead of in-memory Map"
    - "Deterministic session lookup via runtimeKey (hash of seed + sessionId) works correctly"
    - "Existing v2 handler tests pass without modification"
  artifacts:
    - path: "src/lib/v2/storage/prismaSessionStore.ts"
      provides: "PrismaSessionStoreBackend implementing SessionStoreBackend interface"
      exports: ["PrismaSessionStoreBackend"]
      min_lines: 80
    - path: "src/lib/v2/sessionStore.ts"
      provides: "setSessionStoreBackend export for swapping backends"
      contains: "setSessionStoreBackend"
    - path: "prisma/migrations/"
      provides: "Database migration files"
      contains: "migration.sql"
  key_links:
    - from: "src/lib/v2/storage/prismaSessionStore.ts"
      to: "src/lib/prisma/client.ts"
      via: "Prisma client import"
      pattern: "import.*prisma.*from.*prisma/client"
    - from: "src/lib/v2/storage/prismaSessionStore.ts"
      to: "src/lib/v2/sessionStore.ts"
      via: "SessionStoreBackend interface implementation"
      pattern: "implements SessionStoreBackend"
    - from: "src/server/index.ts"
      to: "src/lib/v2/sessionStore.ts"
      via: "Backend swap at startup"
      pattern: "setSessionStoreBackend"
---

<objective>
Migrate SessionStore backend from in-memory Map to Prisma-backed PostgreSQL persistence while preserving deterministic behavior

Purpose: Enables persistent session storage across server restarts and multi-device access without changing handler logic
Output: Working PostgreSQL session persistence with all existing v2 handler tests passing
</objective>

<execution_context>
@C:\Users\estac\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\estac\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-foundation/02-RESEARCH.md
@.planning/phases/02-backend-foundation/02-01-PLAN.md
@.planning/phases/02-backend-foundation/02-02-PLAN.md

# Critical preservation targets
@src/lib/v2/sessionStore.ts
@src/lib/v2/api/sessionHandlers.ts

# Prisma schema and client
@prisma/schema.prisma
@src/lib/prisma/client.ts

# Runtime key utilities
@src/lib/runtime/runtimeKey.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run database migration and verify schema</name>
  <files>prisma/migrations/</files>
  <action>
Initialize local PostgreSQL database and apply Prisma migrations.

**Local database setup (if not running):**
1. Check if PostgreSQL is installed: `psql --version`
2. If not installed, provide instructions but do NOT install (user may prefer Docker)
3. Create .env file based on .env.example with actual DATABASE_URL
4. Example: `DATABASE_URL="postgresql://postgres:password@localhost:5432/ev_trainer"`

**Run migration:**
1. Generate initial migration: `npx prisma migrate dev --name init`
2. This creates prisma/migrations/ directory with SQL files
3. Applies migration to database specified in DATABASE_URL
4. Regenerates Prisma Client with updated types

**Verify migration:**
1. Check that migration files exist in prisma/migrations/
2. Run `npx prisma studio` briefly to verify tables exist (User, Session, SessionEntry, SpotStat, DailyStat)
3. Close Prisma Studio

**Important:** If user doesn't have PostgreSQL installed, log instructions but do NOT fail. They can use Docker or cloud PostgreSQL. The key is that migration command succeeds IF database is available.
  </action>
  <verify>
Check that prisma/migrations/ directory contains migration files.
Run `npx prisma migrate status` to verify migration applied.
Optionally: Run `npx prisma studio` to visually inspect tables.
  </verify>
  <done>
prisma/migrations/ directory exists with initial migration SQL files.
Migration successfully applied to database (if available).
Prisma Client regenerated with schema types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement PrismaSessionStoreBackend</name>
  <files>src/lib/v2/storage/prismaSessionStore.ts, src/lib/v2/sessionStore.ts</files>
  <action>
Implement Prisma-backed SessionStoreBackend following research Pattern 3 (Prisma-Backed SessionStore Implementation).

**src/lib/v2/storage/prismaSessionStore.ts:**

1. Import SessionStoreBackend, SessionRecord, SessionEntry types from ../sessionStore
2. Import prisma from ../../lib/prisma/client
3. Import Prisma types (@prisma/client)

4. Define PrismaSessionStoreBackend class implementing SessionStoreBackend:

**get(key: string): Promise<SessionRecord | undefined>**
- Query: `await prisma.session.findUnique({ where: { runtimeKey: key }, include: { entries: true } })`
- If not found, return undefined
- Map Prisma model to SessionRecord interface:
  - Parse filters field from JSON to SpotFilterInput
  - Parse currentSpot field from JSON to Spot | null
  - Map entries array from Prisma SessionEntry[] to SessionEntry[]
    - Parse spot field from JSON to Spot
    - Parse result field from JSON to DecisionGrade | undefined
- Return mapped SessionRecord

**set(key: string, value: SessionRecord): Promise<void>**
- Use upsert pattern: `await prisma.session.upsert({ where: { runtimeKey: key }, create: {...}, update: {...} })`
- Create block: All fields from SessionRecord
  - runtimeKey: key
  - sessionId, seed, mode, packId, filters (as JSON), decisionIndex, decisionsPerSession, currentSpot (as JSON), isComplete: false
- Update block: decisionIndex, currentSpot (as JSON)
- Handle entries separately:
  - Delete existing entries: `await prisma.sessionEntry.deleteMany({ where: { sessionId: session.id } })`
  - Create new entries: `await prisma.sessionEntry.createMany({ data: value.entries.map(...) })`
  - Map entries to include spot (as JSON), result (as JSON)
- Wrap in transaction: `await prisma.$transaction([...])`

**clear(): Promise<void>**
- Delete all sessions: `await prisma.session.deleteMany()`
- Cascade deletes entries automatically

**async delete(key: string): Promise<void>**
- Delete session by runtimeKey: `await prisma.session.delete({ where: { runtimeKey: key } })`

5. Export PrismaSessionStoreBackend class

**src/lib/v2/sessionStore.ts:**
1. Add export function setSessionStoreBackend(newBackend: SessionStoreBackend): void
2. Implementation: `backend = newBackend;`
3. This allows swapping backend at runtime without changing handlers

**Critical preservation:**
- ALL existing sessionStore functions (createSessionRecord, getSessionRecord, updateSessionSpot, appendSessionEntry) remain UNCHANGED
- Only the backend variable and setSessionStoreBackend are new
- Deterministic properties preserved: same seed + sessionId MUST produce same runtimeKey and retrieve same session
  </action>
  <verify>
Run `npx tsc --noEmit` to check for type errors.
Create test file that instantiates PrismaSessionStoreBackend and tests get/set methods.
Run existing v2 handler tests (if they exist) to ensure no breaking changes.
  </verify>
  <done>
src/lib/v2/storage/prismaSessionStore.ts exports PrismaSessionStoreBackend implementing SessionStoreBackend.
src/lib/v2/sessionStore.ts exports setSessionStoreBackend function.
All methods (get, set, clear, delete) properly map between Prisma models and SessionRecord interface.
JSON serialization preserves Spot and DecisionGrade structures.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Prisma backend into server startup</name>
  <files>src/server/index.ts</files>
  <action>
Swap SessionStore backend to Prisma at server startup.

**src/server/index.ts:**
1. Import setSessionStoreBackend from ../lib/v2/sessionStore
2. Import PrismaSessionStoreBackend from ../lib/v2/storage/prismaSessionStore
3. Before app.listen(), add:
   ```typescript
   // Swap session store backend to Prisma
   const prismaBackend = new PrismaSessionStoreBackend();
   setSessionStoreBackend(prismaBackend);
   console.log('Session store backend: Prisma (PostgreSQL)');
   ```
4. This ensures all handler calls use PostgreSQL instead of in-memory storage

**Verification approach:**
1. Start server with `npm run server:dev`
2. Use curl to POST /api/session/start with valid payload
3. Check that session appears in database (use Prisma Studio or psql)
4. Stop and restart server
5. Use curl to POST /api/session/next with same sessionId and seed
6. Verify session persists across restart (deterministic lookup works)

**Important:** All existing handler tests should pass WITHOUT modification. If they fail, the implementation breaks determinism or changes interfaces.
  </action>
  <verify>
Start server and create a session via API.
Check database for Session record using `npx prisma studio`.
Restart server and retrieve session via API.
Verify same sessionId + seed retrieves correct session state.
  </verify>
  <done>
src/server/index.ts swaps backend to PrismaSessionStoreBackend at startup.
Sessions persist to PostgreSQL database.
Server restart does not lose session state.
Deterministic session lookup (runtimeKey from seed + sessionId) works correctly.
All v2 handler logic preserved without changes.
  </done>
</task>

</tasks>

<verification>
1. Run `npx prisma migrate status` - migration applied successfully
2. Run `npm run server:dev` - server starts with Prisma backend
3. Test session creation: `curl -X POST http://localhost:4000/api/session/start -H "Content-Type: application/json" -d '{"seed":"test123","mode":"TRAINING"}'`
4. Check database: `npx prisma studio` shows Session record
5. Restart server and test session retrieval - deterministic lookup works
6. Run existing v2 handler tests (if any) - all pass without modification
</verification>

<success_criteria>
- PrismaSessionStoreBackend implements all SessionStoreBackend methods
- Sessions persist to PostgreSQL and survive server restarts
- Deterministic behavior preserved (same seed + sessionId retrieves same session)
- No changes to v2 handler logic or interfaces
- JSON serialization handles Spot and DecisionGrade correctly
- Server startup swaps backend seamlessly
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-foundation/02-03-SUMMARY.md`
</output>
