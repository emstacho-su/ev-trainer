---
phase: 08-statistics-analytics
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/stats/components/FilterBar.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can filter stats by position (UTG/HJ/CO/BTN/SB/BB) via multi-select"
    - "User can filter stats by scenario type (RFI/FacingOpen/3Bet/BlindDefense) via multi-select"
    - "User can filter stats by street (preflop/flop/turn/river) via multi-select"
    - "Selected filters persist in URL search params and apply across all dashboard components"
  artifacts:
    - path: "src/app/stats/components/FilterBar.tsx"
      provides: "Position, scenario, and street filter controls alongside existing date filters"
      contains: "positions"
  key_links:
    - from: "FilterBar.tsx"
      to: "URL search params"
      via: "router.push with positions, scenarios, streets params"
      pattern: "params\\.set\\(\"positions\""
    - from: "PerformanceChart.tsx / PositionHeatmap.tsx / SessionHistory.tsx"
      to: "/api/stats/*"
      via: "searchParams.get('positions') passed to fetch URL"
      pattern: "searchParams\\.get\\(\"positions\"\\)"
---

<objective>
Add position, scenario type, and street filter controls to the FilterBar component, wiring them into URL search params so all dashboard components automatically filter their API requests.

Purpose: Closes verification gap "User can filter statistics by date range, position, pot type, street" -- currently only date range is implemented.
Output: FilterBar.tsx with 3 additional multi-select filter groups that persist in URL and propagate to all stats API calls.
</objective>

<execution_context>
@C:\Users\estac\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\estac\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-statistics-analytics/08-VERIFICATION.md

@src/app/stats/components/FilterBar.tsx
@src/app/stats/components/PerformanceChart.tsx
@src/app/stats/components/PositionHeatmap.tsx
@src/app/stats/components/WeaknessBreakdown.tsx
@src/app/stats/components/SessionHistory.tsx
@src/app/stats/components/FlaggedHandsList.tsx
@src/server/controllers/stats.controller.ts
@src/lib/stats/aggregation.ts
@src/lib/stats/types.ts
@src/lib/v2/config/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add position, scenario, and street filter controls to FilterBar</name>
  <files>src/app/stats/components/FilterBar.tsx</files>
  <action>
Add three multi-select filter groups to FilterBar.tsx below the existing date range controls:

1. **Position filter** -- Toggle-chip style buttons for each position: UTG, HJ, CO, BTN, SB, BB. Multiple can be selected. When none are selected, all positions are included (no filter). Use the same toggle-chip pattern from the lobby GameSetup component (Set-based selection). Store as comma-separated `positions` URL search param (e.g., `positions=UTG,CO,BTN`).

2. **Scenario type filter** -- Toggle-chip buttons for: RFI, Facing Open, 3-Bet, Blind Defense. Same multi-select pattern. Store as comma-separated `scenarios` URL search param.

3. **Street filter** -- Toggle-chip buttons for: Preflop, Flop, Turn, River. Same multi-select pattern. Store as comma-separated `streets` URL search param.

Layout: Add a second row below the date presets row using flex-wrap. Group each filter section with a subtle label (text-xs text-slate-500 uppercase). Use the same button styling as date presets but slightly smaller (px-2.5 py-1 text-xs). Selected state: bg-blue-600 text-white. Unselected: bg-slate-800 text-slate-300 hover:bg-slate-700.

Read current filter state from URL search params on mount (parse comma-separated strings back to Set). When any chip is toggled, update URL search params via router.push (same pattern as updateDateRange). When a filter group has zero selections, remove that param from URL entirely.

Import position constants from `@/lib/v2/config/types` (ConfigPositions array: ["UTG", "HJ", "CO", "BTN", "SB", "BB"]). Define scenario and street arrays inline as constants:
- SCENARIOS: ["RFI", "FacingOpen", "3Bet", "BlindDefense"] with display labels ["RFI", "Facing Open", "3-Bet", "Blind Defense"]
- STREETS: ["preflop", "flop", "turn", "river"] with capitalized display labels

Add a "Clear filters" text button at the end of the filter row that removes all non-date filter params.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors. Visually inspect the component renders without errors by checking the dev server if running.
  </verify>
  <done>
FilterBar renders position, scenario, and street toggle chips. Clicking chips updates URL search params. Reloading page restores filter state from URL. Clear filters button removes all non-date params.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire filter params into all dashboard fetch components</name>
  <files>
    src/app/stats/components/PerformanceChart.tsx
    src/app/stats/components/PositionHeatmap.tsx
    src/app/stats/components/WeaknessBreakdown.tsx
    src/app/stats/components/SessionHistory.tsx
    src/app/stats/components/FlaggedHandsList.tsx
  </files>
  <action>
Each of these components already reads `startDate`/`endDate` from searchParams and passes them to their fetch URL. Extend each to also read and pass `positions`, `scenarios`, and `streets` params.

For each component:
1. Read `positions`, `scenarios`, `streets` from `searchParams.get(...)` (they will be comma-separated strings or null).
2. When building the fetch URL params, append these if non-null: `params.set("positions", positions)`, `params.set("scenarios", scenarios)`, `params.set("streets", streets)`.
3. Add these param values to the useEffect/useCallback dependency array so components re-fetch when filters change.

**ALSO FIX** the auth token bug found by the verifier: In PositionHeatmap.tsx and WeaknessBreakdown.tsx, change `localStorage.getItem("accessToken")` to `localStorage.getItem("access_token")` (matching the correct key used by SessionHistory and FlaggedHandsList).

Note: The `streets` param is new and not currently parsed by the stats controller. Add it to the `parseDateFilters` function in `src/server/controllers/stats.controller.ts` by parsing `query.streets` the same way as positions/scenarios. Also update the `StatsFilters` type in `src/lib/stats/types.ts` to include `streets?: string[]`. The aggregation layer does not need to filter by street yet (no street field on DailyStat/SpotStat) -- the param is passed for future use and will be available when postflop stats are added.

Files to modify:
- `src/app/stats/components/PerformanceChart.tsx` -- add positions/scenarios/streets to fetch params and dependency
- `src/app/stats/components/PositionHeatmap.tsx` -- add positions/scenarios/streets to fetch params and dependency; FIX token key
- `src/app/stats/components/WeaknessBreakdown.tsx` -- add positions/scenarios/streets to fetch params and dependency; FIX token key
- `src/app/stats/components/SessionHistory.tsx` -- add positions/scenarios/streets to fetch params and dependency
- `src/app/stats/components/FlaggedHandsList.tsx` -- add positions/scenarios/streets to fetch params and dependency
- `src/lib/stats/types.ts` -- add `streets?: string[]` to StatsFilters
- `src/server/controllers/stats.controller.ts` -- parse streets param in parseDateFilters
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors across all modified files. Grep for `"accessToken"` in stats components to confirm the token bug is fixed (should return zero matches).
  </verify>
  <done>
All stats dashboard components pass position, scenario, and street filter params to their API calls. Changing a filter chip in FilterBar causes all visible components to refetch with the new filter values. The accessToken bug in PositionHeatmap and WeaknessBreakdown is fixed.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. FilterBar renders 3 filter groups (positions, scenarios, streets) with toggle chips
3. Clicking a position chip adds `positions=UTG` (etc.) to URL
4. All chart/heatmap/session components include filter params in their fetch calls
5. `grep -r "accessToken" src/app/stats/` returns zero matches (token bug fixed)
6. Page reload preserves filter selections from URL
</verification>

<success_criteria>
- FilterBar has working multi-select controls for positions (6 options), scenarios (4 options), and streets (4 options)
- Filter state persists in URL search params
- All 5 dashboard data components pass filter params to their API fetches
- Token key bug is fixed in PositionHeatmap and WeaknessBreakdown
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-statistics-analytics/08-08-SUMMARY.md`
</output>
